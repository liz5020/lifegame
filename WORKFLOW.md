# 《人生草稿》開發協作邏輯

> 這份文件寫給你審核：我（Claude）在這個專案裡會怎麼做事。如果哪一條你覺得不對、或跟你想的不一樣，
> 直接說，我改這份文件，不會自己默默照自己認為對的方式做。
>
> **`協作流程說明-共同基準.md` 是claude.ai Project跟Claude Code雙方都保留的同一份基準文字**（分工、唯一正本、交接機制、安全規則）。這份WORKFLOW.md是基準之上的詳細版SOP，兩者原則上不衝突；如果發現寫法對不上，以共同基準那份為準，並回頭修正這裡。

---

## 1. 資料夾結構

```
lifegame/
├── 協作流程說明-共同基準.md          ← claude.ai Project與Claude Code雙方共同保留的基準文字，優先於CLAUDE.md/WORKFLOW.md
├── CLAUDE.md                       ← Claude Code每次啟動自動讀取的濃縮版規則（費用鐵律、架構決定速查）
├── life-sim-design/                 ← 唯一設計權威來源（見第5節，故事定案後由我依你貼的結論寫入；2026-09-20拆檔，取代原單一檔案life-sim-full-design-doc.md）
│   ├── 00-總覽.md                   ← 總目錄、全域更新日誌、設計精神說明
│   ├── 01-敘事生成規則.md ~ 11-高張力抉擇機率判定.md  ← 一章一檔，對應原一~十一章
│   ├── 12-職涯系統.md                ← 9/20新增
│   ├── 13-健康衰退與老年階段.md      ← 9/20新增（與12同日由不同協作對話平行完成，退休段落與12.10重疊待整併）
│   └── 14-內容範例庫/                ← 原十二章拆成子資料夾，依生命階段再分檔（9/20因新增12、13兩章順延為14）
├── index.html                     ← 唯一正式原型檔案（不再分「正式版」「測試版」兩份檔案）
├── CHANGELOG.md                   ← 每次代碼/結構異動的紀錄（我維護，只留最近的日期）
├── CHANGELOG-archive.md           ← CHANGELOG.md切分出去的舊日期封存（目前是2026-09-14），內容仍有效可查證，只是不放在主檔避免每次都要讀過
├── WORKFLOW.md                    ← 這份文件（完整版SOP，CLAUDE.md是它的濃縮速查版）
├── qa/                             ← QA報告、測試手冊
│   ├── 人生草稿_QA_測試手冊_v1.md   ← 測試方法主軸；最後一節「附錄：目前實作落差清單」是落差清單，我持續更新（章節編號可能隨內容調整而變動，以檔案裡實際的章節標題為準，不要死記編號）
│   └── QA_34附錄_歷史封存.md        ← 34節裡篇幅長、內容穩定的子節（目前是34.1完成清單、34.7 B7測試草案）切分出去的完整內容，主檔34節只留摘要+連結
├── content-team/                  ← 內容組提供的原始docx/文件（如有）
├── snapshots/                      ← 設計文件的版本快照（見第5節，用於偵測你更新了哪裡）
└── archive/                        ← 封存的舊版本檔案，不會再被讀取或引用
```

**2026-09-16確認的持續累加型檔案切分慣例**：`CHANGELOG.md`、QA手冊34節都是「只增不減」的紀錄，長期會一直變長，每次同步近況都要重讀全部內容很耗token。規則：主檔只留最近/仍在變動的內容，穩定不太需要重看的舊內容切到同資料夾的另一個檔案（檔名帶「archive」或「歷史封存」字樣），主檔留一句話+連結指向切出去的檔案。**這跟`archive/`資料夾（封存後不會再被讀取引用）不同**——這裡切出去的內容仍然有效、仍可能被查證引用，只是不放進日常同步就要讀的主檔。之後這兩個檔案（或其他類似性質的檔案）如果又累積長了，比照同樣邏輯繼續往同一份封存檔續補，不需要每次都問。

**2026-09-15確認，不採用的兩個提案**：網頁版曾提出用`.claude/agents/dev.md`／`qa.md`子代理分工、以及`docs/handoff-queue.md`佇列檔案交接（範本存在`archive/life-sim-claude-code-starter-template/`）。已評估後決定不採用：
- 子代理：這專案規模用不到平行處理的效益，反而每次都要重新交代脈絡；單一連續對話的記憶延續性更重要（實測案例：處理QA回報時能自己發現並修正先前一版的邏輯錯誤，靠的正是連續對話記憶，換成子代理會失去這個）
- handoff-queue.md：我們現有「你貼原文→我草擬【定案】寫進設計文件→你確認」的分工已經跑過很多輪、驗證有效，不需要改成「網頁版自己寫好定案文字排隊、Code只負責讀取」這種分工；「待實作項目」的需求已經由QA手冊34.2節的落差清單涵蓋，不需要多開一個檔案

**為什麼 `index.html` 只有一份，沒有分「開發部版」「測試部版」兩個檔案：**
檔案內建 `USE_MOCK` 開關與一個 Debug 數值面板（右下角🛠，只在 `USE_MOCK=true` 時出現）。
「開發」跟「測試」的差別不是兩份不同檔案，而是**同一份檔案的兩種模式**：

| | USE_MOCK | Debug面板 | 誰在用 |
|---|---|---|---|
| 我平常改代碼、驗證邏輯 | `true` | 有 | 我（測試部視角） |
| 你之後正式上線 | `false` | 自動不會出現 | 玩家（開發部產出的最終成品） |

之前會搞不清楚哪份才是正式檔，就是因為維護了兩份幾乎相同的1450行檔案、彼此可能漸漸長歪。改成一份檔案 + 一個開關，從根本上不會再有「哪份才是對的」這種問題。

---

## 2. 「開發部」「測試部」怎麼在紀錄裡呈現一目瞭然

都是我，但我會在 `CHANGELOG.md` 裡明確標身分，讓你回頭看時知道這行是「改了什麼」還是「驗證了什麼」：

- **〔開發部〕**：實際修改 `index.html` 的代碼
- **〔測試部〕**：跑 `USE_MOCK=true` 離線模擬、驗證分支、寫測試報告（放 `qa/` 資料夾）
- **〔整理〕**：資料夾結構、文件搬移，非遊戲邏輯本身

一次工作通常是「開發部改完 → 測試部驗證 → 回報你」，會在 `CHANGELOG.md` 裡看到對應的連續兩三筆紀錄。

---

## 3. 費用控制鐵律（最優先，不因任何理由讓步）

- 所有測試、驗證、除錯，預設一律 `USE_MOCK=true`，不需要問你
- 我絕對不會自己把 `USE_MOCK` 關掉去打真實API——不管理由是什麼（想看真實文筆效果、想確認 prompt 效果……都不行）
- 只有進入你我都同意的「正式版本測試」階段，且每一次要打真實API之前，我都會先問：「這次要花費用呼叫真實API來測試ＸＸ，是否允許？」——**每次都重新問**，即使你上次同意過
- 拿不準算不算「正式測試階段」，一律當作還在免費測試階段處理

---

## 4. 標準工作流程

**步驟0、自動落差掃描（2026-09-20新增，取代由你口頭描述改動內容）**

你說「幫我看一下現在進度」「開始做」之類的話，或請我動手做任何事情之前，不用你先講清楚改了哪裡，我會自己先做：

1. 讀`life-sim-design/`底下所有章節檔案（若尚未拆檔則讀`life-sim-full-design-doc.md`），找出所有標記【定案】的規則
2. 逐一對照`index.html`現有代碼，判斷這條【定案】規則是否已經真的實作、實作是否完整、有沒有跟目前代碼邏輯衝突或矛盾
3. 整理成清單給你看，分三類：已實作且正確／已實作但不完整或有落差（具體說哪裡不對）／完全沒實作
4. 你確認要處理哪一項落差之後，才進入下面1~6的標準流程

**這個掃描不用你每次主動要求**——每次你請我做任何事情之前，我會先檢查「距上次掃描後，設計文件的【定案】項目有沒有新增或修改」。判斷方式：比對`life-sim-design/00-總覽.md`全域更新日誌裡晚於上次掃描日期的條目；現在專案已經有git版本控制，也可以用`git log --oneline <上次掃描時的commit>..HEAD -- life-sim-design/`輔助交叉核對，確認沒有漏掉直接手動編輯、沒走網頁版交接流程就修改文件的異動。如果發現有新增或修改，我會先主動提一句「距上次掃描後，設計文件多了/改了哪些定案項目，要不要先掃描一次確認落差」，讓你決定要不要先做這個掃描，不會自己默默略過這一步直接動手改代碼。

「上次掃描記錄」這個狀態統一記在`CLAUDE.md`「標準工作流程」第0步底下（避免兩份文件各存一份、之後對不上），每次掃描完會更新那裡的日期，這裡不重複記錄。

---

**步驟1~6（掃描完、你確認要處理的項目之後才進入這裡）**：

1. **讀設計文件對應章節**——不管功能大小，動手前先讀 `life-sim-design/` 資料夾對應章節檔案的相關段落，不靠印象、不用之前對話記住的版本（文件可能已經被你更新過）
2. **改代碼**——只改這次要做的範圍，不順手重構、不加沒被要求的功能
3. **離線模擬驗證**——`USE_MOCK=true`，自己跑過幾輪、模擬不同分支（不同教養風格/家境/依附軸組合等），確認邏輯有沒有跑對，不會改完就直接丟給你
4. **回報**——這次做了什麼、驗證過哪些情境、有沒有發現設計文件本身邏輯上的漏洞或矛盾
5. **記錄**——寫進 `CHANGELOG.md`（見第2節格式）
6. **部署提醒**——如果這次改動需要部署（Worker 或 Pages），會照現有方式操作，並提醒你需要手動做的步驟（目前 Pages 還是手動上傳）

---

## 5. 你之後更新故事內容（設計文件）時，我們怎麼合作

**背景**：故事主軸的討論在網頁版Claude進行（比較適合天馬行空聊創意），改代碼跟測試在這邊（Claude Code）進行，避免像以前那樣要在兩個對話框之間手動搬訊息、怕漏掉。

**流程**：
1. 你在網頁版跟Claude討論故事/規則，聊到覺得可以定案了
2. 把那段討論的**原文或結論**直接貼給我（不用先自己整理成【定案】格式）
3. 我會：
   - 判斷這屬於設計文件的哪一章、哪一節
   - 草擬成文件既有的【定案】格式，並在 `00-總覽.md` 的「全域更新日誌」加一條對應紀錄
   - 直接寫進 `life-sim-design/` 資料夾對應章節檔案
   - 把「這次新增/修改了什麼」原文貼回來給你確認——你只需要看這段有沒有寫歪，不用重新翻整份文件
4. 你確認沒問題（或跟我說哪裡要改，我當場修）之後，這條就算正式定案
5. 接著我會照第4節標準流程處理：讀文件對應章節→改代碼→`USE_MOCK=true`驗證→回報→記CHANGELOG
6. 每次改完文件，我會把當下整個 `life-sim-design/` 資料夾存一份快照到 `snapshots/life-sim-design_YYYY-MM-DD[序號]_說明/`（拆檔前是單一檔案快照，2026-09-20起改成整個資料夾一起存，因為改動可能同時涉及多個章節檔案），如果你哪次改成自己直接動手改文件（不透過我），也只要說一聲「我自己改了設計文件」，我會拿最新版跟上一份快照資料夾做diff抓出異動，一樣能接得上，不會漏掉

**原則**：我不會自己發明或補完規則細節寫進【定案】——寫進去的內容必須來自你貼過來的討論結論，如果你貼的內容裡有模糊或矛盾的地方，我會先問清楚再下筆，不會自己猜一個版本定案。

---

## 6. 規則不清楚或有矛盾時

不自己猜一個合理版本寫進代碼。會明確列出來問你：
- 這是哪一條規則、在文件第幾節
- 具體卡在哪裡（沒寫清楚，還是跟另一條規則衝突）
- 你確認後更新文件，我再重新讀取實作

---

有任何一條想改，直接說，我會更新這份文件本身。
