# 人生草稿——開發/測試異動紀錄（歷史封存：2026-09-14）

> 這份文件是`CHANGELOG.md`切分出來的舊日期封存區，內容仍然有效、仍可能被引用查證（跟`archive/`資料夾「封存後不會再被讀取或引用」的舊版本檔案性質不同）。
> 只是因為`CHANGELOG.md`本體只保留「最近幾天」的紀錄，避免每次同步近況都要讀過全部歷史，才把已經穩定不太需要重看的日期搬來這裡。
> 需要查更早的細節（例如某個bug當初怎麼修的、某個公式驗證過哪些測試點）時才需要打開這份檔案。

---

## 2026-09-14

**〔整理〕資料夾重整**
- 建立 `qa/`（QA報告與測試手冊移入）、`snapshots/`（設計文件版本快照）、`archive/`（舊版本封存，原 `old/` 內容移入）
- 詳細結構與用途說明見 `WORKFLOW.md` 第一節

**〔整理〕合併重複的原型檔案**
- 原本 `index.html`（正式版，USE_MOCK=false）與 `index test.html`（測試版，USE_MOCK=true + Debug數值面板）兩份檔案，除了 `USE_MOCK` 值跟 Debug 面板之外，遊戲邏輯完全相同
- Debug 面板本身已經有 `if(!USE_MOCK) return` 的自我閘門，正式模式下本來就不會出現，沒有必要維護兩份幾乎一樣的1450行檔案（會有分岔風險——這正是這次搞不清楚哪份才是正式檔的原因）
- 合併為單一 `index.html`：保留 Debug 面板程式碼，`USE_MOCK` 預設 `true`
- 合併前的 `index test.html` 備份於 `archive/index-test-with-debug-panel_pre-merge_20260914.html`
- 已用 Node 做基本語法檢查（`JS_SYNTAX_OK`），尚未在瀏覽器實測

**〔開發部〕USE_MOCK 改回 true**
- 檔案：`index.html` 第730行
- 原狀態：`false`（會直接呼叫真實API），使用者確認這不是有意保留的狀態
- 改為 `true`，符合費用控制鐵律的預設狀態
- 不涉及遊戲規則邏輯，無需離線模擬驗證

**〔測試部〕與設計文件的落差清單（分析，未動代碼）**
- 完整比對 `life-sim-full-design-doc.md`（讀取版本存檔於 `snapshots/life-sim-full-design-doc_2026-09-14.md`）與代碼實作程度
- 結論已在對話中回報給使用者，尚未排入實際修改；後續動工時逐項在此記錄
- 摘要：P1級落差（完全沒實作）＝1.1.3自由輸入字數限制、4.3事件狀態機制、第二章學期節奏與考試算法、1.2.6兩層骰的客戶端機率區間層；其餘中低優先落差詳見對話紀錄，之後動工時會逐項搬進本檔案

---

## 2026-09-14（續）

**〔開發部〕P1-1：自由輸入字數限制**
- 檔案：`index.html`
  - 新增常數 `CUSTOM_INPUT_MAX_CHARS = 300`
  - `.custom-row input` 移除原本 `maxlength="40"`（會靜默擋輸入，違反規則）
  - 新增 `.input-error` CSS 樣式與對應的 `#custom-input-error` 顯示區塊
  - `bindPlaying()`：送出前檢查 `val.length > 300`，超過則顯示「這段行動描述太長了（目前X字，上限300字），請稍微縮短後再送出」且不清空輸入框、不呼叫 `takeTurn`；未超過才正常送出並清空輸入框
- 對應設計文件：一、1.1.3 自由輸入字數限制（【定案】至少100字/理想200-300字；超過須明確提示、不可默默截斷）
- 驗證方式：Node語法檢查通過（`JS_SYNTAX_OK`）；手動追蹤兩條分支邏輯（超過300字／未超過）確認符合預期行為
- **未完成的驗證**：這個session瀏覽器自動化工具未開啟（無chromium-cli/puppeteer/playwright，Claude in Chrome工具本session未啟用），沒有做到真正的瀏覽器點選驗證。使用者已知悉並選擇自行開檔測試，不需要我補做
- 發現的設計文件本身漏洞：無

## 2026-09-14（續2）

**〔測試部〕四條做事態度光譜精確公式驗證**
- 對應設計文件：三、3.7.3 各光譜的機制掛勾（審慎/成就傾向/責任感/團隊vs單打獨鬥）
- 方法：Node腳本（`vm`模組載入`index.html`的script內容，不經瀏覽器），對`buildUserMessage()`與`applyResult()`直接餵入固定測試值，逐一比對設計文件給的對照表數字，排除mock模式隨機亂數造成的雜訊
- 發現：**審慎、成就傾向這兩條的倍率值目前只會被送進真實API的payload，`applyResult()`跟mock引擎完全沒有讀取套用**——這兩條公式對mock模式下的實際遊戲數值變化沒有任何影響，要驗證AI是否真的照這個值調整敘事結果，需要呼叫真實API（屬於之後的正式測試階段，這次未執行）
- 結果：四條公式的**數值計算本身**全數與文件對照表相符
  - 審慎→detail_variance_multiplier：9個測試點（0/30/40/50/60/70/80/90/100）全部✅
  - 成就傾向→achievement_probability_modifier_pp：3個測試點（0/50/100）全部✅
  - 成就傾向→achievement_health_cost_multiplier：4個測試點（30/50/70/100）全部✅
  - 責任感→人脈加分/扣分倍率：5個測試點（50/60/70/80/100）×加分/扣分兩方向，全部✅
  - 團隊vs單打獨鬥→人脈/才識此消彼長：4個測試點（50/60/80/100）全部✅
- 測試腳本：`/private/tmp/claude-501/-Users-rong-----claude/59bc7aa0-70a6-45ad-81cb-84c09558e36e/scratchpad/test-conscientiousness.cjs`（scratchpad暫存，非專案正式檔案）
- 發現的設計文件本身漏洞：無（公式本身定義清楚，问题出在代碼只實作了「傳給AI」沒實作「AI是否真的遵守」的驗證管道，這是實作面的待補項，不是文件邏輯問題）

## 2026-09-14（續3）

**〔開發部〕修正 achievement_probability_modifier_pp 缺少上限保護**
- 檔案：`index.html` system prompt（`buildSystemPrompt()`內，約第589行）
- 問題來源：使用者提供的 `qa/QA測試報告_審慎與成就傾向_20260913.md` 第3.2節指出，這個機率修正值沒有像`detail_variance_multiplier`、`achievement_health_cost_multiplier`一樣加上0.5~1.5的clamp保護；基礎成功機率若夠高（85%以上）疊加高成就傾向（+15pp），數學上會出現超過100%的機率，完全依賴AI自己合理化
- 修法：在system prompt裡明確加一句「加總後不得超過95%、也不得低於5%，超過或低於這個範圍一律視為95%/5%」，純文字補充說明，不改變任何既有【定案】規則、不改任何JS邏輯
- 驗證方式：Node語法檢查通過。**這個修正的效果本身無法用USE_MOCK驗證**——因為這個值只會被送進真實API的payload，mock引擎完全不讀取，要驗證AI是否真的遵守這條新加的95%/5%上限，需要真實API呼叫（正式測試階段才會做）
- 對應設計文件：無直接對應章節（文件1.2.6兩層骰、3.7.3都沒有明講這個邊界情況），這是QA測試發現的實作面缺口，不涉及故事規則變更，因此未經網頁版故事討論流程，直接修正

## 2026-09-14（續4）

**〔整理〕落差清單併入QA測試手冊**
- 檔案：`qa/人生草稿_QA_測試手冊_v1.md`，新增「三十五、附錄：目前實作落差清單」章節
- 原因：「設計文件vs代碼落差清單」原本只存在對話紀錄裡，沒有持久化成檔案，容易遺失或跟QA手冊的測試案例對不上
- 做法：不動手冊原本1-34節的編號與內容，只在檔案最後附加新章節，往後每完成一輪開發都會回來更新這裡的狀態，取代原本只活在對話裡的版本
- 新章節內容：已完成且驗證通過清單、P1完全沒實作、P2有做但不一致、P3影響較小、免費可測尚未測試清單，並逐項標註對應本手冊T01-T20測試編號

## 2026-09-14（續5）

**〔整理〕QA測試手冊全面清理（依審閱建議逐項處理）**
- 檔案：`qa/人生草稿_QA_測試手冊_v1.md`
- 清理項目與順序：
  1. **移除13處破損引用標記**：文件裡散落的`fileciteturn...`格式亂碼（實際內含Unicode私用區隱藏字元，肉眼看不出來但會干擾閱讀），逐一用regex精準移除，同時清掉因此產生的空行，不影響其餘內容
  2. **合併買房重複章節**：原本「十、測試G」情境3（買房）跟「十七、測試N：房屋系統」內容大量重疊，改成G只保留情境1/2（入職/搬家），買房詳細測試統一併入十七
  3. **整合三份測試順序建議為一份**：原本「二十九、①-⑫步驟」「三十、T01-T20清單」「附錄：Day1-3執行方案」三處各自涵蓋範圍不一致（例如①-⑫提到的分支持續性/長期平衡在舊T01-T20清單裡沒有對應編號），合併成一張表（新T01-T21，含詳細章節連結、通過條件、建議時程），移除另外兩處，後續章節編號三十一~三十五依序往前遞補為三十~三十四
  4. **補上T19遺漏的極端角色子測試**：原T19只寫「極端全100」，detail章節（二十五）其實還有「全0」「矛盾角色」兩種測試，合併時已在新T19裡一併列出三種
  5. **新增debug面板現況標註**：第三節（Debug模式需求）補註目前debug面板實際做到哪些欄位、哪些還沒做（日期/固定支出/NPC/已完成事件），3.2節「指定執行某事件」標註為「未來加分項，非近期必要項」
  6. **修正一處我自己先前寫錯的交叉引用**：34.1節原本寫「見本手冊第一節測試方法說明」，但方法A/B的說明其實在另一份`QA測試報告_審慎與成就傾向_20260913.md`裡，已修正引用對象；並在「三十三、v1結論」加一句話說明這節是刻意呼應前面兩節的收斂摘要，不是遺漏檢查
- 結果：全文從1591行降到1540行，章節從一~三十五（35章）減為一~三十四（34章），驗證：章節編號連續無跳號、`filecite`殘留數=0
- 未變動：原文一~二十八節（除買房合併、debug標註外）的測試方法論、公式、預期結果內容本身沒有改寫，只處理結構性重複與雜訊

## 2026-09-14（續6）

**〔整理〕設計文件定案：兩個QA發現的處理方向**
- 檔案：`life-sim-full-design-doc.md`
- 來源：使用者在網頁版討論完發現1（出社會零消費最優）、發現2（幸福感/結局收斂）後，把結論貼回來，依WORKFLOW.md第5節流程寫入
- 新增/修改內容：
  1. 3.1數值總覽：幸福感補上「限制在0-100之間」
  2. 3.4.8：改名「基本生活開銷」，範圍從「僅學生時期」擴大為「所有人生階段」，判斷依據從「是否有正職(occupationCategory)」改為「居住/生活狀態」，新增依居住狀態分級的基準表（住家裡/租屋/買房/與家人伴侶共同生活/退休/特殊狀態），金額暫不定死，標記【待驗證】待模擬後校準
  3. 新增7.1.4「死亡結局的人生標籤判定機制」：明確標註原本「由上到下第一個符合就採用」的結局判定邏輯從未經過設計定案，正式定案改為「每個候選標籤各自算符合分數、取最高者，同分用細節骰」，並要求用少量核心/次要條件的簡單加分制（不做人生輪廓相似度模型）
  4. 1.3更新日誌新增兩條對應紀錄
- 未寫入設計文件的部分：**人脈長期飽和**——使用者明確要求獨立列為另一個平衡問題追蹤，不併入這次幸福感/結局的定案內容，因為目前沒有決定具體規則要怎麼改，先記錄進落差清單/QA手冊追蹤，等後續模擬結果出來再討論
- 快照：已存 `snapshots/life-sim-full-design-doc_2026-09-14b.md`
- 下一步（使用者要求的順序）：先依這些新定案修代碼規則 → 用USE_MOCK大量跑完整人生模擬（觀察25/30/40/60歲財富分布、結局分布、人脈長期趨勢）→ 最後才用真實API驗證敘事層是否遵守規則。目前仍停在「已寫入設計文件、等待使用者確認文字」階段，尚未開始改代碼

## 2026-09-14（續7）

**〔開發部〕實作兩個QA發現的定案內容（3.4.8 / 7.1.4）**
- 檔案：`index.html`
  - 新增 `LIVING_COST_PRESETS`（low/mid/high三組參數）、`LIVING_COST_SCALE`（可切換）、`livingCostTierForState()`、`computeBaseLivingCost()`：基本生活開銷改依居住狀態分級，`occupationCategory`不再是開關
  - `applyResult()`：`livingCost`計算改用`computeBaseLivingCost(s)`；`s.happiness`加上`clamp(0,100)`
  - 新增 `ENDING_DEFINITIONS` 陣列與重寫 `computeEndingTitle()`：改為每個候選結局各自算核心(3分)/次要(1分)條件分數，取最高分，同分用`pick()`隨機決定，取代原本「由上到下第一個符合」
- 對應設計文件：三、3.4.8（2026-09-14改版）、三、3.1、七、7.1.4（新增）
- 驗證方式：Node語法檢查通過；用USE_MOCK跑60+局大規模模擬（30局測結局/人脈/幸福感分布，30局×3種LIVING_COST_SCALE測25/30/40/60歲財富分布，含模擬合理就業/購屋/婚姻軌跡，避免測試artifact），0個異常（NaN/數值越界/崩潰）
- 結果摘要：幸福感確認不再超界；結局種類從1種增為4種；LIVING_COST_SCALE參數確認對負債發生率有預期方向的影響；人脈長期飽和問題確認與此次修正無關、持續存在，依使用者指示獨立追蹤（見QA手冊34.6）

**〔測試部〕T07/T10(機械部分)/T15/T21 驗證**
- 方法：沿用Node+vm sandbox模式，直接呼叫`advanceStructuredTime()`/`demoteStaleCharacters()`/`applyResult()`等函式餵入固定測試值
- 結果：全數通過，詳見QA手冊34.1

**〔整理〕QA手冊34節更新第二輪測試結果**
- 檔案：`qa/人生草稿_QA_測試手冊_v1.md`
- 34.1新增本輪所有測試結果；34.3買房項目拆分機械部分(已驗證)與門檻檢查(未實作)；34.5改寫為「需真實API或被缺失功能卡住」清單；新增34.6「已知但獨立追蹤的平衡問題」記錄人脈長期飽和、兩種結局從未觸發、結局分布仍偏態、財富數字偏高等觀察

## 2026-09-14（續8）

**〔整理〕內容組正式回覆文件歸檔**
- 檔案：`人生草稿_測試結果整併建議與定案_v1.docx`、`人生草稿_開發參數規格_v1.docx`、`人生草稿_下一輪60局模擬測試表_v1.docx`（使用者已放在專案根目錄）
- 內容：內容組將QA的兩個發現+人脈飽和問題整併，編號為A1-A4（人脈飽和/家庭圓滿過高/壯志未酬體衰力竭缺失/60歲存款偏高）、B5-B8（伴侶折減/基本開銷金額/結局條件/退休state）、C9（審慎成就傾向真實API驗證），並明定優先順序：先修A2→重跑60局→才調A1/A3/A4→最後重做結局模型

**〔開發部〕實作A2：核心數值邊際遞減與人脈閒置衰退**
- 對應設計文件：新增三、3.5.3（係數皆標記【待驗證】測試參數，不得由代碼自行升格定案）
- 檔案：`index.html`
  - 新增 `applyMarginalDiminish(rawDelta, currentValue)`：正向成長量 × (100-目前值)/100，只影響正向delta
  - 套用在才識(`knowledgeDelta`)、人脈(`networkDelta`)成長，健康不受影響（照doc2明確要求）
  - 新增人脈閒置衰退：`state.networkIdleStreak` 追蹤連續無人脈經營行為的回合數，連續6回合後每3回合衰退1-3分
  - 新增可切換開關 `ENABLE_MARGINAL_DIMINISH`／`ENABLE_NETWORK_IDLE_DECAY`，並在debug面板加上對應checkbox，方便回歸比較（照doc2「十五、開發完成標準」要求）
- 驗證：先跑30局聚焦驗證（baseline/社交高投入/社交低投入），確認人脈≥95比例97%→10%後，再補跑完整60局（6組×10局，含事業高投入/極度節省/高消費，追蹤25/30/40/60歲存款/人脈/才識/健康/幸福感、整局最低值、結局分布、長期數值到達時間表）
- 結果：A2各項doc3驗收標準全數通過；意外發現A1（家庭圓滿77%→50%）與A3（壯志未酬/體衰力竭從0次→各5次/2次出現）都隨A2改善而連帶好轉，證實內容組「A2是上游根因」的判斷；同時診斷出「壯志未酬」的5次觸發其實核心條件從未真正滿足（整局最低幸福感從未低於35），是靠次要條件+平手隨機骰選中，指向mock負面事件強度不足，非門檻本身問題
- 0個異常（NaN/越界/崩潰），詳細數據見QA手冊34.6
- 未處理：A1門檻（照指示暫不調）、A4的3×3敏感度分析（尚未實作SALARY_SCALE）、B7結局核心/次要條件正式重新設計（架構層級改動，尚未開始）、C9真實API驗證（需使用者另外明確同意花費用）

## 2026-09-14（續9）——【測試第一版基準點】

**〔開發部〕實作①資產模型：現金/房產淨值/房貸餘額拆分**
- 對應設計文件：新增三、3.4.9（房貸換算標記【待驗證】測試參數）
- 檔案：`index.html`
  - `state.savings` 拆分為 `cash`、`propertyValue`、`mortgageBalance` 三個獨立欄位，全檔案所有引用點（`computeFinancialHealth`、`ENDING_DEFINITIONS`、UI顯示、debug面板、snapshotForUndo）已同步更新
  - 買房：從`cash`扣頭期款，建立`propertyValue`/`mortgageBalance`（簡化假設：20年期、不含利息、8成貸款成數）
  - 每月結算：`mortgageBalance`隨房貸支出攤還遞減，還清時自動移除「房貸」支出項
  - 賣房/轉租：`propertyValue`扣除`mortgageBalance`後的淨值變現為`cash`，兩欄位歸零
  - 新增`SALARY_SCALE`、`LIVING_COST_MULTIPLIER`兩個倍率參數（A4敏感度分析用），debug面板可即時調整
- 驗證：固定測試值驗證買房/攤還/還清/賣房四種情境全數✅；用新欄位重新執行A4 3×3敏感度分析（9格×15局），拆出淨資產/現金/房產淨值/買房比例
- 結果：確認財富分布主要由薪資主導（0.6×→1.0×漲124%），基本生活費影響小很多（1.0×→2.0×僅降7%）
- 已知副作用：買房現在真的扣頭期款，現金不足會變負數，凸顯既有「買房頭期款門檻檢查」P2缺口的必要性，這次未修

**〔測試部〕A1對照測試、A3 mock工具修正**
- A1：兩組極端路徑對照（獨行俠/不婚不生/高職涯 vs 高社交/穩定伴侶/婚姻生育，各20局）——家庭圓滿佔比5% vs 70%，分流明顯，支持「聚合50%不需要調門檻」的判斷
- A3：修正mock的`happiness_delta`，加入8%機率雙向重大事件（挫折/驚喜各半，期望值抵銷，呼應6.1防壓抑原則），讓mock能覆蓋真正的幸福感低谷
  - 過程記錄：第一版修正誤用單向負面事件，導致期望值被拖成負的、A1測試數據異常，已自行發現並改為雙向版本重跑
- 詳細數據見QA手冊34.6

**〔測試部〕B7結局重新設計 —— 測試草案，未寫入設計文件**
- 內容組提供五種結局第一版設計草案（必要資格制，取代分數制），明確要求先用mock測試、不要先寫進`life-sim-full-design-doc.md`第七章
- 純測試腳本（不動`index.html`正式邏輯）驗證：家庭圓滿、體衰力竭兩條必要資格可用現有資料測試（分別42/60、0/60合格）；事業巔峰、壯志未酬兩條**無法測試**——需要人生履歷(chronicle)語意化紀錄，但mock目前只產生「（模擬事件）一件小事」這種無語意內容的文字，真實API是否足夠具體也未知
- 「同時合格幾種結局」的完整統計因此做不出來，已誠實回報數據缺口而非用近似值頂替
- 詳細見QA手冊34.7

**〔整理〕定調測試第一版（Test v1）**
- `index.html` 加上版本註解標記範圍：A2、3.4.8、3.4.9、7.1.4（維持現行，未套用B7草案）
- 設計文件1.3新增對應更新日誌，快照存至 `snapshots/life-sim-full-design-doc_2026-09-14d.md`
- QA手冊34節版本紀錄同步標記測試第一版基準點

## 2026-09-14（續10）

**〔開發部〕USE_MOCK 切換為 false —— 使用者要求，自行手動測試真實API**
- 檔案：`index.html` 第730行
- 情境：使用者明確要求開啟，範圍是「自己手動玩、自行觀察」，不是我方跑自動化批次測試，因此費用完全由使用者自己控制
- 提醒使用者：debug面板（🛠）只在`USE_MOCK=true`時才會建立，切換後面板會消失；且今天資料結構有變動（savings拆成cash/propertyValue/mortgageBalance），建議先清空存檔再開新局
- 測試完畢後記得切回`true`，避免下次忘記又意外呼叫真實API

## 2026-09-14（續11）

**〔開發部〕USE_MOCK 切回 true —— 使用者測試完畢**
- 檔案：`index.html` 第730行，恢復離線模擬模式預設值，不會再呼叫真實API
- Debug面板（🛠）恢復顯示

## 2026-09-14（續12）——學生財富系統改版 + 4個bug修正

**〔開發部〕①學生時期財富系統改版（正式定案，非草稿）**
- 對應設計文件：3.4.1改版、新增3.4.10、新增五、5.5
- 檔案：`index.html`
  - 零用錢改遊戲內單位：`STUDENT_ALLOWANCE_UNITS`（清寒30/小康60/富裕120），`rollEconomicTier()`/`rollEconomicTierForced()`同步改用固定單位值，政治世家設150（延續「略高於一般富裕」的既有設計精神，非硬性規定，已標註）
  - 存款下限保護：settlement與one_time_transaction兩處都加上`!s.occupationCategory`（學生）時的下限0判斷；一次性消費超額直接不執行（`return`跳過），不是扣成負值
  - 新增`computeMealCareEligible(familyStructure, parentInfos)`：完整實作資格判定表（雙親同住/隔代教養/服刑中/單親喪親/自營業者例外規則等），新增`presentParentInfos`追蹤陣列（跟既有`presentParentStyles`分開，不干擾原有依附風格骰定邏輯）
  - 新增遊戲流程階段`"lifestyle"`：`renderLifestyleChoice()`/`bindLifestyleChoice()`，插在setup（開局揭曉）與playing之間，玩家選花錢習慣+三餐方式後才進入`startLife()`
  - `computeBaseLivingCost()`分支：學生用新公式（零用錢×花錢習慣%×三餐倍率），已就業維持3.4.8既有的居住狀態分級公式
  - 新增解鎖機制：`mealFullyUnlocked`/`mealJustUnlocked`，觸發點為`college_location_choice`變成外縣市/出國，或第一次`job_change`；新增`renderMealUnlockModal()`跳出重新選擇彈窗
  - `buildUserMessage()`新增payload欄位：`is_student`、`spending_habit`、`meal_arrangement`、`meal_event_multipliers`；system prompt新增對應說明（單位制敘事用語、存款下限敘事配合、三餐情境倍率）
  - 新增`cookingInterestSeed`旗標（興趣系統伏筆，目前不觸發效果）
- 驗證：Node固定測試值全數驗證通過——零用錢單位對應、11種家庭組合的資格判定、消費超額不執行、月結算鎖0、已就業者不受限制、開銷公式計算、解鎖觸發、結算餘額快照
- 已知簡化/待確認：與伴侶共同生活的開銷折減沿用既有0.75倍率（非本次新規範圍）；世代傳承(`succeedAsChild`)因缺乏結構化教養風格/職業資料，簡化為預設可選「家裡包辦」

**〔開發部〕②修正4個bug（人生草稿0914測試.pdf）**
1. **結算存款顯示未更新**：根因是render時每則歷史log都讀取最新的`s.cash`而非各自時間點的值。修正：`applyResult()`結算後把`balanceAfter`存進`lastSettlement`並隨log entry保存，render改讀取`e.settlement.balanceAfter`
2. **數值顯示未四捨五入**：`ledgerRow()`與結局畫面的健康/才識/人脈顯示都加上`Math.round()`，不影響底層浮點精度，只處理呈現層
3. **手足姓名性別不一致**：發現這其實是client端代碼本身的bug——`SIBLING_NAMES`混男女名字、跟`SIBLING_RELATIONS`各自獨立`pick()`，無配對邏輯。修正：拆成`SIBLING_NAMES_MALE/FEMALE`、`SIBLING_RELATIONS_MALE/FEMALE`，新增`rollSibling()`先骰性別再配對；`succeedAsChild()`裡的手足轉換也同步修正（依原本`relation`推斷性別再配對稱謂）。另外也強化了system prompt裡1.2.5規則的措辭，加上具體錯誤範例，因AI生成的NPC姓名一致性仍需要真實API驗證才能確認
4. **技術錯誤訊息外洩**：`takeTurn()`的catch區塊改為先重試一次API呼叫，重試後仍失敗才顯示中性容錯敘事「旁白剛剛恍神了一下，這回合沒講清楚，稍後再試一次看看」，不再附加`e.message`或任何原始破碎內容
- 驗證：1、3項已用固定測試值驗證；2項為呈現層改動，邏輯簡單無需額外測試；4項的重試邏輯結構已確認，實際錯誤情境需真實API測試才能完整驗證
