# 人生草稿——開發/測試異動紀錄

> 這份文件記錄每一次對 `index.html` 或資料夾結構的實際改動。設計規則本身的異動記錄在
> `life-sim-design/00-總覽.md` 的「全域更新日誌」，兩份文件分開維護，不要混在一起寫。
> （2026-09-20更正：原本寫的是拆檔前的`life-sim-full-design-doc.md`「1.3 更新日誌」，該檔案已封存到`archive/`。）
>
> 每筆記錄格式：日期 / 身分（開發部・測試部・整理）/ 做了什麼 / 對應設計文件章節（如適用）/ 驗證方式。
>
> **這份檔案只保留最近的紀錄。2026-09-14（含所有「續」）已搬到 `CHANGELOG-archive.md`**——內容還是有效、可查證，只是平常同步近況用不到，先切開避免每次都要讀過全部歷史。之後如果這份檔案本體又累積到一定長度，會再往`CHANGELOG-archive.md`續補（同一份封存檔案往下加，不再另開新檔）。

---

## 2026-09-15

**〔開發部〕新增每回合可見數值變化顯示**
- 使用者需求：每次選項增加哪些可見數值都要顯示出來，跟財富結算小字同一種呈現方式
- 檔案：`index.html`
  - `applyResult()`開頭記錄`beforeVisibleStats`快照（健康/才識/人脈/表達力/外表），結尾比對算出`statChanges`（已含邊際遞減、責任感/團隊倍率、人脈閒置衰退等所有效果套用後的最終差值，四捨五入後只保留真的有變化≠0的項目），存進log entry
  - 新增`renderStatChanges()`，正向數值teal色、負向stamp色，跟財富結算小字並列顯示在每則日記下方
- 驗證：Node固定測試值✅——健康/人脈原始delta正確顯示；才識顯示的是套用完wisdom/logic乘數與邊際遞減後的最終值（3，不是AI原始給的8），符合「顯示最終實際變化」的需求；表達力0.2四捨五入為0時正確被過濾掉不顯示

## 2026-09-15（續）

**〔整理〕評估並回覆網頁版提出的Claude Code專案範本**
- 來源：使用者收到網頁版準備的`life-sim-claude-code-starter (1)`資料夾，內含`.claude/agents/dev.md`／`qa.md`、`CLAUDE.md`、`docs/handoff-queue.md`概念、`qa-reports/`
- 決定（使用者授權由開發部主導結構性判斷）：
  - ✅ 採用CLAUDE.md：已建立於專案根目錄，濃縮版規則+架構決定速查，Claude Code自動讀取
  - ❌ 不採用dev/qa子代理：維持單一連續對話，理由見WORKFLOW.md第1節
  - ❌ 不採用handoff-queue.md：維持現有「使用者貼原文→我草擬定案寫進設計文件」分工，QA手冊34.2已涵蓋待實作清單需求
  - 範本裡的舊版`index.html`（1450行）、`life-sim-full-design-doc.md`（1189行）皆已封存到`archive/life-sim-claude-code-starter-template/`，避免跟現行版本混淆
  - 附帶發現：範本提及「兩邊共用git歷史」，但本專案目前不是git repository，此點不成立，是否要init git列為獨立、之後再議的決定
- 已同步更新`WORKFLOW.md`記錄此決定與理由，供之後回顧

## 2026-09-15（續2）

**〔整理〕建立雙方共同基準文字，確保網頁版與Claude Code資訊對稱**
- 檔案：`協作流程說明-共同基準.md`（使用者從網頁版取得後存入專案根目錄）
- 內容逐條核對跟dev端實際決定完全一致（不用子代理、不用handoff-queue.md、待實作追蹤沿用QA手冊、CLAUDE.md+WORKFLOW.md分層、qa報告命名慣例、USE_MOCK安全規則、git初始化列未來獨立討論）
- 修正一處日期誤植：版本記錄原寫2026-09-13，實際定案討論發生於2026-09-15，已更正並註明
- `CLAUDE.md`、`WORKFLOW.md`都加上指向這份文件的說明：協作流程若要調整，先改這份共同基準，不在CLAUDE.md/WORKFLOW.md裡各自發展不同措辭，避免兩邊分岔

## 2026-09-15（續2）——事件狀態機制（P1，QA-0001）

**〔開發部〕實作四、4.3事件狀態機制**
- 對應設計文件：四、4.3、新增4.3.1（首版實作範圍）
- 檔案：`index.html`
  - 新增`MILESTONE_DEFS`（10個里程碑，分`auto:true`客戶端自動判斷/`auto:false`AI回報兩類）、`initMilestones()`、`refreshMilestoneLocks()`
  - `state.milestones`新增於`newRoll()`/`succeedAsChild()`（世代傳承重新初始化，不繼承上一世紀錄）
  - 自動判斷5項：高中/大學畢業掛在`advanceStructuredTime()`的階段轉換點；首次正職掛在`job_change`的`isFirstJob`；買房/搬出家裡掛在`housing_choice`（明確排除「住家裡」不算搬出去）
  - AI回報5項：新增JSON schema欄位`milestone_updates`，`applyResult()`驗證只能改AI_REPORTABLE清單內的id、只能設completed/skipped、不能繞過locked狀態
  - `buildUserMessage()`payload新增`milestone_status`（10個里程碑目前狀態），system prompt新增完整規則說明+已完成/跳過不得重新觸發的強制指示
- 過程中抓到一個排序bug：`refreshMilestoneLocks()`原本寫在AI回報處理**之後**，導致同一回合內用「上一輪」的舊locked狀態去擋AI的合法回報（即使這回合玩家已經滿足解鎖年齡）。已修正為先刷新鎖定狀態再處理AI回報
- 驗證：Node固定測試值8項全數✅——初始鎖定狀態、鎖定解除、AI合法回報、AI無法繞過locked、AI無法竄改auto項目、5個自動判斷情境全數正確、completed狀態跨回合不被重置、payload正確帶出10個里程碑狀態
- 已知簡化：信用卡/結婚決定的18歲解鎖門檻是實作假設，非文件明文規定，已標記【待驗證】；清單本身標記為【待補充，首版範圍】非封閉清單

## 2026-09-15（續3）——買房頭期款門檻檢查（P2轉正）

**〔開發部〕實作七、7.5.3買房頭期款門檻檢查**
- 對應設計文件：7.5.3補充實作細節
- 檔案：`index.html`
  - `applyResult()`housing_choice區塊：`nowOwn && !wasOwn`時先算出這次買房需要的頭期款，`s.cash < 頭期款`就整個`housing_choice`不處理（不扣款、不建房產/房貸、里程碑不標記），現金/房產狀態完全維持原樣
  - `buildUserMessage()`payload新增`max_affordable_home_monthly_payment`（=現金÷60）、`home_purchase_min_down_payment_pct`，system prompt新增對應說明，要求AI敘事不要寫出超出負擔上限的購屋成功
- 過程中抓到一個算式錯誤：頭期款公式是「月付×240×0.25＝月付×60」（20年期、8成貸款成數），我第一版payload提示值誤用÷48計算，會讓AI收到錯誤的負擔上限、寫出之後會被系統擋下的敘事。已修正為÷60，並新增交叉驗證測試（用AI提示的max_affordable反推去買房，確認一定會成功）
- 驗證：Node固定測試值全數✅——現金不夠正確擋下（房產/現金/里程碑皆不變）、現金剛好足夠成立、現金充裕正常扣款、4組不同現金水位的提示值反推驗證全部一致
- 對應QA手冊34.3的P2項目，此次補上後移至34.1

## 2026-09-15（續4）——學期節奏+考試算法（P1）

**〔開發部〕重寫時間segment系統，實作二、2.2學期節奏與2.4考試成績算法**
- 對應設計文件：新增2.2.1、2.4.1（首版實作範圍）
- 檔案：`index.html`
  - 原本8段/年、每回合固定跳1段的簡化模型，改成12段/年（上下學期各6段：開學初/考前準備期/期中考/期中後/期末考/假期），每段有回合預算（取文件範圍上限值：2/6/1/6/1/8），期中考期末考本身budget=1，本身就是揭曉成績的里程碑回合
  - 新增`SEMESTER_PHASES`/`YEAR_SEGMENTS`、重寫`computeTimeLabel()`、`advanceStructuredTime()`（含一般行動的`turnsInSegment`累加+跳過語意的整段跳躍+`detectExamCrossed()`偵測跳過時是否跨過考試）、新增`advanceStageYear()`（畢業里程碑判斷從此處理）
  - 新增`detectStudyIntent()`偵測讀書關鍵字，`state.studyCountThisTerm`累積讀書次數，考試觸發後歸零
  - 新增考試相關payload欄位：`exam_triggered`/`exam_type`/`exam_score_hint`（=才識+讀書次數×3）/`study_count_this_term`，system prompt新增對應指示（大方向層由客戶端算，AI在附近生成細節層，保留意外機率）
- 驗證：Node固定測試值全數✅——段落預算正確累加不提前跳段、期中考確實在第7回合(6準備+1考試)觸發、跳過語意正確偵測跨過的考試、年度輪轉+畢業里程碑正常、畢業倒數標籤正確、payload欄位正確；另外用50回合乾淨模擬驗證一整學年剛好4次考試(上下學期各1次期中+1次期末)，時機精確吻合
- **⚠️ 重大節奏影響，已在設計文件2.2.1標記待確認**：文件給的回合預算數字（取上限）疊加到高中3年+大學4年共7年學校生涯，總計約336回合，是原本簡化模型（約56回合）的6倍。這是文件既有【定案】數字直接相乘的結果，不是實作自行加長，但需要使用者/內容組確認總量是否符合預期節奏
- 已知簡化：「每次讀書+3分」的考試分數公式係數是測試參數，非文件規定；讀書次數判斷用關鍵字比對玩家行動文字，非結構化語意理解

## 2026-09-16

**〔整理〕切分持續累加型的長檔案，降低同步近況的token成本**
- 使用者發現讀檔耗token偏高，主因是`CHANGELOG.md`、QA手冊34節都是只增不減的紀錄，每次同步都要重讀全部歷史
- 做法：
  - `CHANGELOG.md`：2026-09-14（含全部12則「續」）搬到新檔`CHANGELOG-archive.md`，主檔只留2026-09-15起的紀錄+格式範本
  - QA手冊34節：34.1（已完成清單）、34.7（B7結局測試草案）兩節篇幅長且內容穩定，完整內容搬到新檔`qa/QA_34附錄_歷史封存.md`，主檔對應章節只留摘要+連結；34.2/34.3/34.4/34.5/34.6篇幅短或仍在變動追蹤，維持原樣
- `WORKFLOW.md`第一節補上新檔案說明+切分慣例（跟`archive/`資料夾「封存後不再讀取」性質不同，這裡切出去的內容仍有效可查證，只是不放進日常同步的主檔），往後類似情況比照辦理不需要每次問
- 驗證：純文件搬移整理，未動`index.html`代碼，逐一確認搬移後章節編號/連結對得上、無內容遺漏

## 2026-09-16（續）

**〔整理〕寫入設計文件定案：人生節奏總覽（整體回合節奏+學生時期課業/興趣系統+死亡機制改版）**
- 來源：使用者在網頁版討論完後貼回的「人生節奏總覽」交接文字，依標準流程判斷章節、草擬【定案】格式寫入`life-sim-full-design-doc.md`
- 寫入前發現3處交接文字跟既有文件有衝突/模糊，已用AskUserQuestion請使用者拍板後才動筆：
  1. 新舊死亡機制關係——使用者選「完全取代、合併成單一機率」，但將門檻年齡從原提案的19歲改為**40歲**
  2. 學生時期總回合數——上限值算出378、下限值精確等於336（交接文字寫「與336相近」），使用者選**取上限378**
  3. 才識決定興趣路徑拖累斜率的規則放置章節——使用者選**併入三、3.2才識章節**（新增3.2.4）
- 設計文件異動：
  - 新增二、2.5整體人生階段回合預算（22歲以後加速曲線：找方向期40回合/年逐步降到81歲以後10回合/年，41-50歲刻意打平）+2.5.1說明為何學生時期不套用此曲線
  - 修改2.2/2.2.1：學生時期學期結構改為期中/期末各自有完整準備期（取代原本不對稱結構），回合預算改為378（開學初3+期中準備6+期中考1+放鬆2+期末準備6+期末考1+假期8，×14學期），2.2.1的語氣從「待確認警示」改為「內容組已重新檢視、主動維持」
  - 新增三、3.2.4課業與興趣路徑的成績連動：興趣路徑拖累課業成績，斜率由才識決定、無安全次數上限，具體公式標記【待補充】
  - 新增七、7.1.5死亡機制改版：取代3.1「壽命70-88歲隨機」固定值定義與7.1原本「壽命到達或意外身亡」兩態框架，合併為單一機率判定機制，門檻19→40歲，具體機率公式標記【待補充,下一輪討論】，並明確註記**代碼在新公式定案前維持舊版19歲門檻運作**，避免文件跑在代碼前面卻沒人發現不一致
  - 1.3更新日誌、目錄同步更新；已存快照`snapshots/life-sim-full-design-doc_2026-09-16.md`
- **⚠️ 尚未動`index.html`任何代碼**：這次只完成「網頁版討論→寫入設計文件定案」，標準流程的下一步（讀文件→改代碼→USE_MOCK驗證）尚未開始，下一輪需要實作的項目包括：2.5的22歲以後回合節奏、2.2/2.2.1的學生時期段落結構改版、3.2.4課業/興趣雙軌成長、7.1.5死亡機制門檻從19→40歲（機率公式本身待下一輪討論定案後才能實作）
- 已知會讓舊存檔跑不動的部分：目前只是文件定案，代碼還沒改，暫不影響現有存檔；等下一輪實際動代碼時（尤其學期節奏結構若改動`SEMESTER_PHASES`/`YEAR_SEGMENTS`）會再提醒是否需要清空重來

## 2026-09-16（續2）

**〔整理〕寫入設計文件定案：才識成長公式修正+自律公式修正**
- 來源：使用者貼回QA測試部提出的兩項公式修正結論+測試建議
- 設計文件異動：
  - 修改三、3.2.2才識成長公式：基礎值從寫死的常數5，改為AI標籤`event_type`（ordinary/intensive/milestone）、程式查表決定的「事件原始學習值」，明確接上3.5.3邊際遞減；milestone標籤由程式依`event_id`計數擋下重複觸發（同一人生階段最多1~2次），不由AI自行判斷是否已用過
  - 修改三、3.7.2自律累積來源：確認初始值維持50分中性起點、不受教養風格影響（撤回先前曾提出的「教養風格給定初始基準」提案）；新增「興趣」來源的持續投入判定規則（喜歡但不持續不算，連續3次投入+1、連續6次再+1，由程式計數非AI給值）
  - 3.7.3對應的【待補充】備註加註釐清：3.7.2是「自律本身怎麼成長」、3.7.3待補的是「自律成長後如何反過來加成其他數值」，兩者不是同一件事
  - 1.3更新日誌同步更新；已存快照`snapshots/life-sim-full-design-doc_2026-09-16b.md`
  - QA手冊34.2新增這兩項為P1待實作項目，並把QA測試部給的測試建議完整記錄下來，供之後實作完成才能真正驗證
- **⚠️ 尚未動`index.html`任何代碼**：才識成長從「AI自由給delta」改成「AI只給標籤+程式查表計算」，以及自律機制從零開始實作，都是下一輪才要做的事
- 附帶備註：使用者原本詢問是否要一起加入「興趣系統」，這次貼的內容只涉及才識/自律公式修正（興趣只是其中一個成長來源/事件分類，不是興趣系統本身的完整成長機制），還在等使用者確認是否有更完整的興趣系統設計要一併補上

## 2026-09-16（續3）——興趣系統v1

**〔整理〕寫入設計文件定案：興趣系統v1（獨立成章）+ 章節重編號**
- 來源：使用者貼回網頁版討論好的興趣系統v1完整設計（回應前一輪「才識/自律修正裡的興趣只是分類來源，不是興趣系統本身」的提問）
- 設計文件異動：
  - 新增「八、興趣系統（初版）」獨立章節：8.1觸發與候選成立機制（被動/主動雙軌，AI只分類、程式判定候選成立）、8.2反應三分類（正向/中性/明確負向，取代原「正向2次成立/負面歸零」）、8.3投入度與邊際遞減（0-100，套用與才識/人脈相同公式）、8.4興趣的代價（須至少掛一種成本）、8.5多興趣並存靠行動點稀缺性自然限制、8.6久未投入降階為背景興趣+敘事記憶喚醒、8.7下游出口（自律/職涯權重/副業契機，職涯權重公式與副業時間成本皆標【測試值,非正式定案】）、8.8興趣類別7類（草案）
  - **原「八、內容範例庫」順延為「九、內容範例庫」**：全文所有交叉引用（9.1/9.1.5/9.2.3/9.3.x等）同步改號，用python regex機械替換後逐一核對章節標題與引用皆連續無跳號
  - 順手修正兩處既有懸空引用（非本次規則變動，屬文字校對）：7.5.3「呼應八、8.2職涯不設終身鎖死」應為七、7.2的筆誤；5.4附近依附風格範例段落誤寫「內容範例庫7.1」應為現行9.1
  - 1.3更新日誌同步更新；已存快照`snapshots/life-sim-full-design-doc_2026-09-16c.md`
  - QA手冊34.2新增興趣系統v1為P1待實作項目，34.2同時補上先前漏列的「整體人生階段回合預算/學期結構改版」「死亡機制改版」兩項，測試建議段落擴充興趣部分；34.6的A10狀態從「🔴待內容組確認」改為「🟢已由內容組重新檢視、主動定案，代碼待實作」
- **⚠️ 尚未動`index.html`任何代碼**：興趣系統目前代碼完全沒有對應資料結構（候選興趣、投入度、興趣卡），是全新機制
- 累計至此，2026-09-16這天共完成三批設計文件定案（人生節奏總覽、才識/自律公式修正、興趣系統v1），下一步是否要開始實作，待使用者指示

## 2026-09-16（續4）——才識成長公式修正實作

**〔開發部〕實作三、3.2.2才識成長公式修正（AI事件標籤+程式查表，取代寫死基礎值5）**
- 對應設計文件：三、3.2.2（2026-09-16修正）
- 檔案：`index.html`
  - 新增`KNOWLEDGE_EVENT_VALUES`（ordinary 3~5／intensive 5~8／milestone 10~15，皆標【測試參數,待驗證】）、`KNOWLEDGE_MILESTONE_CAP_PER_STAGE`（取文件1~2次的上限值2）、`rollKnowledgeRawValue(eventType, eventId, s)`：查表決定原始學習值，milestone依`event_id`去重+依`timeState.stageMode`（人生階段）計數擋下超額觸發
  - `applyResult()`知識成長區塊改用`rollKnowledgeRawValue(r.event_type, r.event_id, s)`取代原本直接讀`d.knowledge`（AI自由給的數值），乘數鏈（智慧乘數×邏輯加成×3.7.3團隊乘數）與3.5.3邊際遞減完全不變
  - JSON schema：`stat_deltas`移除`knowledge`欄位，新增頂層`event_type`／`event_id`；system prompt對應說明改寫，明確要求AI只給標籤分類不給數值，milestone需綁定可重複使用的`event_id`
  - `newRoll()`／`succeedAsChild()`兩個角色初始化點都新增`knowledgeMilestoneUsedIds`／`knowledgeMilestoneCountByStage`欄位（世代傳承視為全新一世，不繼承上一世紀錄）
  - Mock測試引擎`mockGenerateTurn()`同步改版：`deltas`不再含`knowledge`，改依`focus==="knowledge"`機率生成`event_type`（15%機率intensive、6%機率milestone並從3個固定id小池子取`event_id`方便測試dedup）
- 驗證：Node固定測試值（vm載入`index.html`的script內容）17項全數✅——區間抽樣、milestone去重（event_id重複/次數上限/跨階段獨立計算）、event_type為空時raw=0、完整`applyResult()`乘數鏈+邊際遞減串接正確、mock版輸出格式正確（milestone必帶event_id、stat_deltas不再殘留knowledge欄位）；測試腳本存於scratchpad，非專案正式檔案
- 已用`node -e`對`<script>`內容做語法檢查，`JS_SYNTAX_OK`
- 對應QA手冊34.2的P1項目，此次完成後移至34.1，完整驗證細節記錄於`qa/QA_34附錄_歷史封存.md`
- **⚠️ 此改動會讓舊存檔跑不動**：角色state新增了`knowledgeMilestoneUsedIds`／`knowledgeMilestoneCountByStage`欄位，且AI回應JSON schema變動（`stat_deltas.knowledge`移除、新增`event_type`/`event_id`），建議清空重來
- 尚未實作（今天定案的其餘三批）：2.2/2.2.1/2.5學期節奏與整體回合預算改版、七、7.1.5死亡機制改版（明確要求先不動代碼）、3.7.2自律的興趣來源成長、八、興趣系統v1整個新機制——下一步視使用者指示決定優先順序

## 2026-09-16（續5）——興趣系統v1 + 自律興趣來源成長實作

**〔開發部〕實作八、興趣系統v1核心機制 + 三、3.7.2自律的興趣來源成長**
- 使用者指示：「相關連的可以一起測試」——自律的興趣來源成長依賴興趣系統本身的候選/連續投入追蹤才有意義，兩者本來就互相依賴，一起做一起測。死亡機制改版（7.1.5）因文件明確要求先不動代碼，這次不碰；學期節奏改版（2.2/2.2.1/2.5）跟這兩個沒有依賴關係，留到下一輪
- 對應設計文件：八、興趣系統（2026-09-16新增）、三、3.7.2（2026-09-16修正）
- 檔案：`index.html`
  - 新增`INTEREST_CATEGORIES`（7類）、`findInterestCard()`、`computeInterestCareerWeightBonus()`、`applyInterestEvent()`（8.1-8.2候選成立+反應三分類+8.3投入度邊際遞減+8.7自律加成/副業契機觸發）、`sweepInterestDecay()`（8.1候選淡出+8.6降階背景興趣）
  - `applyResult()`新增呼叫`applyInterestEvent(s, r.interest_event)` + `sweepInterestDecay(s)`，緊接在conscientiousness_shift處理之後
  - `newRoll()`／`succeedAsChild()`兩個角色初始化點新增`interestCandidates:[]`／`pendingSideBusinessCardId:null`
  - JSON schema新增`interest_event:{"category":null,"reaction":null}`；system prompt新增完整說明（AI只分類、不判斷候選成立/投入度/轉正，且明確要求自律的興趣來源不要透過`conscientiousness_shift.selfDiscipline`重複給分）
  - `buildUserMessage()`payload新增`interest_status`（現有興趣卡類別/狀態/投入度）、`pending_side_business_category`
  - 新增`renderSideBusinessModal()`（比照既有`renderMealUnlockModal()`的modal-backdrop模式），投入度跨過40時在`takeTurn()`結尾跳出三選一（保持興趣原樣／接零星案／正式副業）
  - Mock測試引擎：新增`guessInterestCategory()`關鍵字對應7類，`mockGenerateTurn()`同步輸出`interest_event`（正向70%/中性20%/負向10%）
- 驗證：Node固定測試值（vm載入`index.html`）29+17項全數✅——候選成立(單次正向不成立/連續3次轉正)、反應三分類(中性不增不減/負向降低不刪除)、投入度邊際遞減、自律連續3次+1與6次再+1(含中斷重新起算)、副業契機40門檻+不重複觸發、候選淡出(6回合)、正式興趣卡降階(10回合)+重新投入喚醒、職涯權重公式、`applyResult()`完整整合、payload正確帶出狀態、mock版輸出格式；另外用80回合混合行動端到端模擬，全程無崩潰、無NaN/數值越界。測試腳本存於scratchpad，非專案正式檔案
- 已用`node -e`對`<script>`內容做語法檢查，`JS_SYNTAX_OK`
- **已知未完成/未整合部分**（誠實記錄，非bug）：8.4興趣代價（沒有連動時間/金錢/健康成本效果，文件未給具體公式）、8.7職涯權重函式已寫但代碼裡沒有對應的「職涯分支選擇」決策點可以整合、「正式副業」選擇目前只記錄狀態沒有連動實際數值效果（呼應文件「副業時間成本現階段用敘事語言描述」的指示）。候選轉正門檻(連續3次)、候選淡出(6回合)、正式興趣卡降階(10回合)三個數字皆為實作階段自訂的【測試參數】，文件本身也承認這些數字尚未有共識
- 對應QA手冊34.2的兩個P1項目，此次完成後移至34.1；34.3新增興趣系統未完成部分的追蹤條目
- **⚠️ 此改動會讓舊存檔跑不動**：角色state新增`interestCandidates`／`pendingSideBusinessCardId`欄位，AI回應JSON schema新增`interest_event`，建議清空重來

## 2026-09-16（續6）——學期節奏改版+整體人生階段回合預算實作

**〔開發部〕實作二、2.2/2.2.1學期結構改版 + 2.5整體人生階段回合預算**
- 使用者指示：「全部照字面實作（22-90歲全部改成回合預算制度）」——這是牽動CLAUDE.md既定架構決定的大改動，先用AskUserQuestion跟使用者確認落實範圍後才動手
- 對應設計文件：二、2.2/2.2.1（2026-09-16修改）、2.5（2026-09-16新增）
- 檔案：`index.html`
  - `SEMESTER_PHASES`從6段改為7段：開學初3、期中準備期6、期中考1、**期中後放鬆2（新增，取代原本「期中後~期末前」單一不對稱段落）**、期末準備期6、期末考1、假期8，`YEAR_SEGMENTS`從12段變14段（一學期預算27、一學年54）
  - 所有原本寫死的`%12`/`*12`/`idx<6`（`computeTimeLabel`/`detectExamCrossed`/`advanceStructuredTime`）改成動態讀`YEAR_SEGMENTS.length`/`SEMESTER_PHASES.length`，避免магic number
  - 新增`LIFE_STAGE_ROUND_BUDGET`（22-30/31-40/41-50/51-60/61-70/71-80/81+七個年齡帶，邊界採年齡區間標籤本身而非文件裡可能取整的「年」數欄位）、`roundsPerYearForAge()`
  - `advanceStructuredTime()`的`career`模式整個重寫：從原本「半年為單位、年齡限22-29」的粗顆粒機制，改成「依`roundsPerYearForAge()`查表的每年回合預算，不再限制年齡上限」；`timeState.careerYear`/`careerHalf`欄位移除，改用`adultTurnsInYear`單一計數器
  - `takeTurn()`不再判斷`turnStartAge<=29`才走結構化時間，**全年齡（15歲起）一律呼叫`advanceStructuredTime()`**，取代原本「30歲以後交給AI自由判斷age_advance」的舊架構
  - `computeTimeLabel()`career模式：22-29歲顯示「入職第X年」，30歲以後顯示「XX歲」（呼應一、1.1.2的巨觀時間軸粒度表）
  - system prompt對應段落改寫，移除「15-29歲/30-45歲/46歲以上」的舊速度分段說明，改為「全年齡都已經由系統算好，age_advance一律填0」
- 驗證：Node固定測試值32項全數✅——新學期段落結構、期中考(第10回合)/期末考(第19回合)精確觸發、一學年4次考試、**高中3年+大學4年總回合數精確等於378（54×7），與設計文件2.2.1定案數字完全吻合**、`roundsPerYearForAge()`13個年齡查詢點全數正確、22/30/31歲年齡帶邊界切換正確、career模式跳過語意/時間標籤格式正確；另外用5局完整人生（15歲跑到forceEnding）端到端模擬，全程無崩潰、無NaN/數值越界
- 已用`node -e`對`<script>`內容做語法檢查，`JS_SYNTAX_OK`
- **🔴 重大發現，記入QA手冊34.6 A10並回寫進設計文件2.5.2**：5局端到端模擬顯示，一局完整人生（15歲到死亡約70-78歲）總回合數落在**約2000~2100回合**，即使樂觀估計每回合30秒也要跑16-17小時，跟設計文件2.5.1自己講的「1-2小時一輪」節奏目標明顯矛盾。這不是實作算錯，是2.5表格數字（40/35/35/30/25/18/10回合/年）疊加60年左右成人壽命後的機械結果，性質上跟2.2.1當初發現的336回合問題一樣，只是這次疊加年數更長、問題更明顯。已在文件2.5.2標記【待確認,重大】，需要內容組決定是否大幅下修回合/年數字，或是調整2.5.1的節奏目標措辭
- CLAUDE.md「已知的架構決定」段落同步更新（原本「15-29歲結構化、30歲以後AI自行判斷」的敘述已取代）
- 對應QA手冊34.2的P1項目，此次完成後移至34.1；34.6 A10從「代碼待實作」更新為「學生階段已完成，新發現全人生總回合數問題待內容組確認」
- **⚠️ 此改動會讓舊存檔跑不動**：`timeState`欄位結構變動（`careerYear`/`careerHalf`→`adultTurnsInYear`），且全年齡的時間推進邏輯整個改變，建議清空重來
- 至此，2026-09-16定案的四批項目（人生節奏總覽、才識/自律公式修正、興趣系統v1、學期節奏+整體回合預算）中，除死亡機制改版（7.1.5，文件明確要求先不動代碼）外皆已實作完成

## 2026-09-16（續7）——【測試第二版基準點】

**〔整理〕定調測試第二版（Test v2）**
- `index.html`加上版本註解標記範圍（v1之後新增/改版）：4.3事件狀態機制、7.5.3買房頭期款門檻檢查、每回合可見數值變化顯示、學生財富系統改版+4個bug修正、3.2.2才識成長公式修正、3.7.2自律的興趣來源成長、八、興趣系統v1、2.2/2.2.1學期節奏改版+2.5整體人生階段回合預算。未併入此版：七、7.1.5死亡機制改版（文件明確要求先不動代碼）
- 設計文件1.3新增對應「測試第二版基準點」更新日誌條目，快照存至`snapshots/life-sim-full-design-doc_2026-09-16e_testv2.md`
- QA手冊34節版本紀錄同步標記測試第二版基準點，比照v1當初的格式
- 已知未解問題隨基準點一併標註：全人生總回合數約2000~2100回合、跟2.5.1節奏目標矛盾，待內容組確認（見34.6 A10）
- 使用者接下來要換帳號（切到網頁版/其他Claude帳號）繼續測試討論，這份基準點記錄是交接用的現況快照

## 2026-09-16（續8）——v2.1測試手冊交接，寫入設計文件定案

**〔整理〕寫入設計文件定案：人生草稿v2.1測試手冊（回應2.5.2總回合數矛盾）+ 才識/興趣系統補洞**
- 來源：使用者換帳號到網頁版討論後，貼回完整的「人生草稿v2.1測試手冊」交接文字，正式引入【定案】/【測試參數】/【待確認】三態用語
- 這份交接文字直接回應了上一輪（續7）標記的2.5.2重大問題（全人生2000~2100回合矛盾），內容組選擇「下修22歲以後回合/年數字」（方案a），不是改口徑接受長時數（方案b）
- 設計文件異動（`life-sim-full-design-doc.md`）：
  1. **2.5整體人生階段回合預算數字下修**：22-29歲32/30-39歲28/40-49歲15/50-59歲12/60-69歲10/70-79歲7/80+5回合/年，取代9/16初版的40/35/35/30/25/18/10；年齡帶邊界統一改為10年一段的左閉右開表示法（以生日為界），取代原本31-40是9年、41-50是10年的不對稱分段
  2. **新增2.5.3**：記錄一個文件既有落差——9.2/9.3標題寫大學/技職19-22歲（畢業22歲），但2.5舊版引言寫「學生時期(15-21歲)」（畢業21歲），兩者從未同一輪核對過。這不是本次新決定，v2.1交接文字本身也把這列為【待確認】，**已依CLAUDE.md規則不自行假設任一邊界，僅記錄矛盾待使用者/內容組核對**
  3. **3.2.2補充**milestone上限計數語意（限制對象是人生階段內milestone總觸發次數，不是單一`event_id`各自次數）與AI enum異常值降級規則（非法enum值降級為`ordinary`，不崩潰不猜測）——查證後兩者皆與`index.html`現行實作(`rollKnowledgeRawValue`/`knowledgeMilestoneCountByStage`)一致，純文件澄清，不需改代碼
  4. **8.2補充**候選興趣進度的`positive`=+1／`neutral`=0／`negative`=-1（下限0）規則，取代原本含糊的「暫停或降低」措辭
  5. **8.7補充**副業契機的狀態機語意（`candidate`→`active`→`business_opportunity_available`→`business_declined`/`side_business`，離開available狀態後不重新觸發）與「興趣投入度不得直接轉換為職業能力值/薪資值，只能加權出現機率」——查證後`sideBusinessOffered`布林值與`computeInterestCareerWeightBonus()`皆已滿足，純文件澄清，不需改代碼
  6. **8.4新增**商業交易類（VII類，代購/轉賣/投資）財務事件須獨立風險判定，不可把投入度直接當成功率使用，呼應34.6 A4已抓到的財富膨脹問題
  - 1.3更新日誌同步更新；已存快照`snapshots/life-sim-full-design-doc_2026-09-16f_v2.1.md`
- **🔴 查代碼時發現一個現行實作跟新定案不符的落差（非本次修正範圍，記錄待下一輪處理）**：8.2新定案的候選興趣進度規則要求`negative`只扣1分、下限0，但`index.html`的`applyInterestEvent()`目前是單次`negative`就把`card.positiveStreak`直接reset為0（同時連動3.7.2自律的連續投入次數獎勵一起歸零重新起算）。這跟v2.1交接文字明確要避免的「曾經喜歡過一次卻因單次不好玩就永久消失」正是同一個問題。下一輪改代碼時需要一併修正
- **交接文字裡明確要求本輪不寫入/不假設的項目，維持不動**：
  - 學生畢業年齡邊界（19-21 vs 19-22）——已記入2.5.3待確認，不自行判斷
  - 才識高是否連帶加速興趣成長（新提案，預設不採用）
  - 候選興趣卡「來源描述」欄位合併同主題不同措辭的提案
  - 自律「非核心興趣」縮小適用範圍的提案
  - 才識/自律AI給值具體數值範圍——3.2.2其實已有`ordinary 3~5/intensive 5~8/milestone 10~15`的暫定區間文字（標【待補充,下一輪校準】），不是全新缺口，已在回覆使用者時說明
  - 「人生時間/精力分配系統」——交接文字明確排除在本輪範圍，建議另開一個獨立機制討論，本輪未在資料結構預留`time_cost_level`欄位（等真正要做時再加，非本次任務）
- 對應QA手冊34節：34.2新增本輪P0/P1待實作項目（8.2候選進度負向規則修正）、34.6 A10更新為「數字已下修回應，待重新端到端測試驗證是否真的解決節奏矛盾」、新增一條記錄2.5.3的既有落差
- **⚠️ 尚未動`index.html`任何代碼**：這次只完成「網頁版討論→寫入設計文件定案」，且明確查證了多項條目已被現行代碼滿足（milestone計數/副業狀態機/投入度不轉職業能力），下一輪實際要改代碼的只剩：2.5新回合數字的實作與重新端到端測試、8.2候選進度negative改為-1而非reset歸零

## 2026-09-16（續9）——v2.1兩項代碼實作：回合預算新數字+興趣候選進度規則修正

**〔開發部〕實作2.5整體人生階段回合預算v2.1新數字 + 8.2興趣候選進度negative規則修正**
- 對應設計文件：二、2.5（2026-09-16 v2.1修正）、八、8.2（2026-09-16 v2.1補充）
- 檔案：`index.html`
  1. `LIFE_STAGE_ROUND_BUDGET`表改為v2.1新數字與新邊界：22-29歲32/30-39歲28/40-49歲15/50-59歲12/60-69歲10/70-79歲7/80+5回合/年（10年一段左閉右開），取代原本40/35/35/30/25/18/10、31-40是9年41-50是10年的不對稱舊表。`roundsPerYearForAge()`本身邏輯不變，只改表格數字
  2. `applyInterestEvent()`新增`card.candidateProgress`欄位，專門追蹤candidate→active轉正進度（`positive`+1／`neutral`不變／`negative`-1下限0），跟原本`card.positiveStreak`（供3.7.2自律「連續」投入判定用途）脫鉤成兩個獨立欄位——`positiveStreak`在`negative`時仍照舊整個歸零（自律的「連續」語意v2.1未變動，只有候選進度規則變了），避免我自行延伸沒被交接文字要求的規則
- 驗證：Node固定測試值（vm載入`index.html`的`<script>`內容）23項全數✅
  - 回合預算：14個年齡邊界查詢點（22/29/30/39/40/49/50/59/60/69/70/79/80/120歲）全數對應正確數字，邊界切點精確落在生日交界
  - 興趣候選進度：2次positive後1次negative，candidateProgress從2降為1（非歸零）、positiveStreak歸零為0；candidateProgress從0出發negative仍floor在0；連續3次positive正確轉正為active；「2正+1負+2正」淨進度2-1+2=3一樣能轉正（驗證v2.1要修正的「單次negative永久消失」問題確實解決）；neutral對兩欄位皆不影響；自律「連續」判定不受影響——中斷後仍需重新累積滿3次連續positive才觸發自律加成，不會因candidateProgress沒歸零就提早給分
- 已用`node -e`對`<script>`內容做語法檢查，`JS_SYNTAX_OK`
- **🔴 額外驗證項，重要發現**：用新回合預算表對22歲到65/70/75/78/80/85/90歲等假設死亡年齡做靜態定積分（非完整隨機端到端模擬），總回合數落在**約1234~1404回合**，比v2.1修正前的2000~2100回合下降約3成——方向正確，但樂觀估計每回合30秒仍要**10-12小時**，離2.5.1「1-2小時一輪」的節奏目標還有數量級落差。已回寫進設計文件2.5.2與QA手冊34.6 A10，標記為【待確認,重大】延續追蹤，不宣告已解決
- 對應QA手冊34.2的兩個P0/P1項目，此次完成後移至34.1，完整驗證細節記錄於`qa/QA_34附錄_歷史封存.md`
- **⚠️ 此改動會讓舊存檔跑不動**：興趣卡資料結構新增`candidateProgress`欄位，且22歲以後的回合預算數字全面改變（原本存檔記錄的`adultTurnsInYear`進度會對應到錯誤的新budget），建議清空重來
- **這次只做了靜態算式驗證，不是像先前2.5.2那樣的完整5局端到端隨機模擬**（含死亡機率判定/AI敘事節奏分支），下一輪如果要正式關閉2.5.2追蹤，應該補做一次完整端到端模擬取得更準確數字
- 尚未實作（v2.1交接文字裡明確待確認、本輪未動的項目）：學生畢業年齡邊界（19-21 vs 19-22，見設計文件2.5.3）、才識-興趣成長連動、候選卡來源欄位合併、自律縮小為非核心興趣範圍、人生時間/精力分配系統——皆等使用者/內容組決定後才進入下一輪

## 2026-09-16（續10）——內容組拍板三項決議：2.5.2/2.5.3結案 + 新增九、大學科系系統

**〔整理〕寫入設計文件定案：「一輪」定義確認、學生畢業年齡依科系分流、大學科系系統獨立成章**
- 來源：使用者帶著上一輪Claude Code提出的「一輪可能指session」分析回內容組討論，內容組正式拍板三項決議並貼回定案彙整
- 設計文件異動（`life-sim-full-design-doc.md`）：
  1. **2.5.2結案**：確認「一輪」= 一次遊玩session，不是整局人生。v2.1數字（1234~1404總回合）÷90~120回合/session ≈ 10~14次session跑完一局人生，維持不變，不再下修，解除【待確認,重大】標記
  2. **2.5.3改寫**：學生時期畢業年齡不是單一數字，依科系而定——多數科系22歲畢業（4年制）、醫學/牙醫/獸醫系23歲畢業（5年制），延畢封頂兩年（最晚24/25歲）。這解開了原本「15-21歲 vs 15-18/19-22歲」的文件內部落差——根源是文件假設畢業年齡是單一值，實際設計是依科系分流的區間
  3. **新增「九、大學科系系統」獨立章節**（比照八、興趣系統的章節等級）：9.1規模與定位（不新增回合預算，局部性影響）、9.2科系分類8大類（附對應職業八大類）、9.3下游伏筆權重表（創業/接案/海外/演藝/接家業五條線，高中低權重）、9.4修業年限與畢業年齡、9.5三個動態狀態機（轉系/雙主修/休學肄業，共通四步驟模板：AI鋪陳→程式判定候選→玩家明確選項→兩層骰判定結果）、9.6內容範例庫（待補）、9.7明確待測試項目（5項，交付QA測試部）
  4. **原「九、內容範例庫」順延為「十、內容範例庫」**：全文交叉引用（10.1/10.1.5/10.2.3等）同步更新，含5.2.5/5.2.6/5.4等章節裡指向內容範例庫的懸空引用修正
  - 1.3更新日誌同步更新；已存快照`snapshots/life-sim-full-design-doc_2026-09-16h_v2.2.md`
- 對應QA手冊34節：34.6 A10狀態改為已結案（🟢）；新增34.2 P1項目——大學科系系統的三個動態狀態機（轉系/雙主修/休學肄業）+9.7列出的5項測試建議
- **⚠️ 尚未動`index.html`任何代碼**：這次是設計文件定案，大學科系系統完全是全新機制（科系欄位、下游伏筆權重、三個狀態機），代碼裡目前沒有任何對應資料結構，下一輪才開始實作

## 2026-09-16（續11）——大學科系系統v2（ChatGPT開發審閱補強）+ 實作前鋪墊修正

**〔整理+開發部〕寫入設計文件補強定案 + 實作大學科系系統的兩項前置鋪墊**
- 使用者指示：先處理「跟大學系統相關的有影響的項目」，再開始實作大學科系系統本體
- 來源：使用者貼回「大學科系系統v2（整合ChatGPT開發審閱意見）」文件，抓出兩個開發前必須解決的邏輯衝突（1.1轉系重算年限vs畢業年齡上限互相矛盾、1.2雙主修是否計入延畢未定義），並補上開發參考用的欄位規格、AI/程式責任邊界表、狀態機細節、權重校準方法、QA測試矩陣
- 使用者明確指示：「裡頭的1-1 1-2的問題依建議內容下去實施，也測試過一輪沒問題就ok」

**第一步：`index.html`鋪墊修正（先做，大學科系系統會直接依賴這個基礎）**
- 對應設計文件：九、9.4（修業年限改依角色查表，取代原本寫死的「大學4年」）
- 檔案：`index.html`
  - 新增`s.collegeYearsRequired`（科系基礎修業年限，未實作科系選擇機制前預設4年）、`s.collegeDelayYearsUsed`（已用延畢年數，未實作延畢觸發機制前預設0）兩個角色欄位，皆用`||`預設值處理，不需要改`newRoll()`/`succeedAsChild()`，對舊存檔100%向下相容
  - 新增`COLLEGE_DELAY_CAP=2`、`collegeTotalYearsRequired(s)`（= base + clamp(delay,0,2)）、`collegeYearLabel(s)`（大一~大五，超過基礎年限顯示「延畢第N年」）
  - `advanceStageYear()`的college分支從寫死`yearInStage>4`改為`yearInStage>collegeTotalYearsRequired(s)`；`computeTimeLabel()`同步改用`collegeYearLabel(s)`，「畢業倒數」判斷也從寫死`>=4`改為動態比對`collegeTotalYearsRequired(s)`
- 驗證：Node固定測試值（vm載入`<script>`）13項全數✅——預設4年制角色行為與修正前完全一致（378回合、大四年畢業）、5年制角色正確撐到第5年才畢業且顯示「大五」、延畢1年正確延後畢業並顯示「延畢第1年」、延畢超過封頂2年正確clamp、「畢業倒數」標籤依動態年限正確觸發/不觸發、學生時期單年預算54回合/378總回合數維持不變（回歸測試）

**第二步：實作1.1轉系年齡上限、記錄1.2雙主修時間成本判斷**
- 對應設計文件：九、9.4（轉系最後可行年齡，新增）、9.5.2（雙主修zero-cost，新增）
- 1.1：新增`COLLEGE_GRAD_AGE_CAP={4:24,5:25}`、`isTransferFeasible(currentAge, targetYearsRequired, delayYearsUsed)`——判斷轉系當下年齡+目標科系修業年限+已用延畢年數是否超過目標科系的畢業年齡上限，超過則轉系不成立。已用延畢年數要算進公式，因為它代表已消耗掉多少延畢緩衝額度，轉系不會重置這個額度（設計文件9.4的【待驗證】備註已說明）
- 1.2：雙主修判斷為**zero-cost**（不計入`collegeDelayYearsUsed`，只影響敘事層課業壓力與財務支出）——**這是Claude Code依9.1「不新增回合預算」原則做的判斷，不是內容組正式拍板**，理由是若雙主修也計入延畢，會讓影響範圍從原本只有醫牙獸/延畢玩家擴大到所有雙主修玩家，跟9.1定調的「局部性影響」原則衝突。已在設計文件與代碼註解中明確標記可覆寫，尚未實作雙主修功能本身（狀態機留到下一輪），這裡只是先把決策記錄下來供未來實作依循
- 驗證：Node固定測試值10項全數✅——設計文件範例的「醫牙獸/其餘科系最後可行轉系年齡皆為20歲」精確吻合（age20可行、age21不可行，兩條科系線都驗證）；已用延畢年數會正確侵蝕可轉系的年齡餘裕（例：19歲+1年延畢額度已用，等同20歲無延畢額度的臨界情況）；延畢輸入超過封頂2年時在計算中正確被clamp，不會繼續侵蝕餘裕
- 已用`node -e`對`<script>`內容做語法檢查，`JS_SYNTAX_OK`
- 對應QA手冊34.2大學科系系統項目，`isTransferFeasible()`與修業年限鋪墊已完成並移入34.1；轉系/雙主修/休學肄業三個狀態機本體、8大科系權重表、學生主狀態欄位（9.8）其餘部分仍未實作，等下一輪正式開始「實作大學系統」
- **本輪不影響舊存檔**：新增的兩個角色欄位皆有`||`預設值，不需要初始化就能運作；`isTransferFeasible()`是純函式，還沒有任何呼叫點串接進遊戲流程（轉系功能本身尚未實作），不影響現有存檔行為

## 2026-09-16（續12）——大學科系系統本體實作（①②③④全部完成）

**〔開發部〕實作九、大學科系系統完整機制：學生主狀態模型、選科系、轉系/雙主修/休學肄業三個狀態機、科系權重骨架**
- 使用者指示：照9.9建議的①②③④順序實作
- 對應設計文件：九、大學科系系統全章（9.1-9.9）

**① 學生主狀態模型**
- `newRoll()`/`succeedAsChild()`新增欄位：`studentMajorGroup`/`studentIsFiveYearTrack`/`studentStatus`（null/enrolled/graduated/withdrawn）/`leaveStatus`（normal/onLeave）/`transferCooldown`/`dualMajorStatus`（null/candidate/active/abandoned/completed）/`examBelowExpectationStreak`/`lowCashStreak`/四個`pending*`彈窗旗標/`leaveResumeCountdown`/`universityEventLog`/`lastExamPerformanceLog`
- 新增`MAJOR_CATEGORIES`（9.2的8大類，含科系範例、是否可能落5年制的旗標）、`findMajorCategory()`
- **選科系觸發機制（設計文件未指定，Claude Code的實作決定）**：高中畢業轉大學那一刻（`advanceStageYear()`的highschool→college分支）設`pendingMajorSelection=true`，`takeTurn()`結尾跳出`renderMajorSelectionModal()`讓玩家選8大類之一；選到「醫藥/護理類」會再跳出次選擇（醫學/牙醫/獸醫系5年 vs 藥學/護理/物理治療等其餘科系4年），因為9.4的5年制只精確指名醫學/牙醫/獸醫三個具體科系，不是整個「醫藥/護理類」類別，9.8的欄位表沒有給到這個顆粒度，是實作時發現的落差
- `assignStudentMajor(s, majorKey, isFiveYear)`：寫入科系分類+是否5年制，同步設定上一輪就做好的`collegeYearsRequired`

**② 接上既有考試/才識/財務機制**
- 修業年限/畢業年齡：直接沿用上一輪鋪墊好的`collegeTotalYearsRequired()`/`collegeYearLabel()`/`advanceStageYear()`，這次只是把`collegeYearsRequired`從「恆為4的預設」改成「由選科系結果決定」
- **🔴 過程中發現一個既有小bug，本輪未修正，記錄給下一輪**：`advanceStructuredTime()`在考試觸發當下就會把`studyCountThisTerm`歸零（清空供下學期重新累積），但`buildUserMessage()`的`exam_score_hint`公式（`知識值+studyCountThisTerm×3`）是在這之後才計算，所以AI在考試當回合實際拿到的hint永遠是「歸零後」的值，讀書次數的加成完全沒發揮作用。這個bug在9/15考試算法上線時就存在，跟本次大學系統無關，這次為了轉系候選判定需要正確的讀書次數，在`takeTurn()`裡另外搶在`advanceStructuredTime()`呼叫之前存了一份`studyCountBeforeAdvance`快照給轉系判定用，**沒有動原本payload那處的既有邏輯**，避免無預期影響已測試過的考試行為，該bug修正留給下一輪
- 學費：沿用既有`one_time_transaction`機制（AI自由回報金額），system prompt新增「大學雙主修學分費約每學分NT$2000-2500」的敘事參考。**這跟9.9表格寫的「學費金額由程式負責，AI不可自行算出金額」理想狀態有落差**——本輪判斷這屬於既有財務系統的範圍，不在「大學科系系統」這次的任務內，沒有另外建一套結構化學費計算器，維持現況

**③ 三個動態狀態機**
- **轉系（9.5.1）**：`updateTransferCandidateOnExam()`在考試結算時更新「連續低於預期」計數——用知識值+讀書次數算出的client端proxy分數（不依賴AI敘事內容），低於`TRANSFER_EXAM_EXPECTATION_THRESHOLD`（55，測試參數）記一次，連續2次（9.5.1定案數字）且`transferCooldown`已歸零才跳出`renderTransferOfferModal()`；彈窗會依`isTransferFeasible()`把超出畢業年齡上限的目標科系選項disabled掉。確認轉系後，成功機率**採Claude Code設計的公式**（知識值×0.5+自律程度換算，clamp在10%-95%之間，標記【測試參數】——設計文件9.5.1只說「複用2.4考試成績算法精神」，沒給精確公式，這是延伸實作的判斷），成功則`assignStudentMajor()`+`yearInStage`重設為1（不做學分抵免，呼應定案）
- **雙主修（9.5.2）**：`checkDualMajorCandidate()`才識≥70（測試參數）或依`foreshadowTriggerProbability()`機率觸發候選；`resolveDualMajorOffer()`套用上一輪定案的zero-cost判斷（申請不動`collegeDelayYearsUsed`）。**中途主動放棄（abandoned狀態）本輪未做獨立UI**——`dualMajorStatus`只有candidate/active/completed三種狀態會被實際用到，completed是在`advanceStageYear()`畢業判定時自動把仍是active的雙主修結算掉，abandoned狀態留給資料結構但沒有觸發路徑，是本輪的範圍縮減，設計文件9.5.2本身提到的「中途放棄」內容範例目前也還沒有對應的敘事觸發點
- **休學/肄業（9.5.3）**：`checkWithdrawalCandidate()`沿用既有`s.lowHealthStreak`（health<30累加，這次沒有新建）+新增`s.lowCashStreak`（cash<=0累加，呼應3.4.10學生現金floor在0的既有保護），連續4回合（測試參數）觸發`renderWithdrawalOfferModal()`。「休學一學期」**本輪為簡化實作**：立刻扣0.5延畢年額度（9.8定義的最小粒度）、`leaveStatus`設onLeave並用`leaveResumeCountdown`倒數幾回合後自動復學，**沒有真的暫停學期段落推進引擎**（完整的「休學期間學期完全凍結」需要更大改動，這次先讓機制骨架跑得通）。「正式肄業」：`studentStatus`轉`withdrawn`、`stageMode`直接跳career，呼應7.2職涯不設終身鎖死
- **🔴 測試時抓到並修正一個真的邏輯漏洞**：休學要消耗跟延畢共用的2年額度（9.8明講「不可各自獨立上限」），但最初實作只在記帳數字上clamp，沒有真的擋下「額度用完後還能一直選休學」——雖然數字不會再增加，但玩家仍可無限次選「休學」卡在原地不會真的往下推進學業。已修正：`collegeDelayYearsUsed`達到`COLLEGE_DELAY_CAP`後，彈窗會把「休學一學期」選項disabled，`resolveWithdrawalOffer()`本身也在程式層擋一次（雙重把關，不只靠UI）

**④ 科系權重骨架（未做絕對值校準，比照9.3【待補充】的定調）**
- `MAJOR_FORESHADOW_WEIGHT_TABLE`（9.3原始8×5高中低表格）+`foreshadowTriggerProbability(majorGroup, line)`採「共同基準機率×類別倍率」算法（9/16 ChatGPT審閱建議），`FORESHADOW_BASE_RATE`/`FORESHADOW_WEIGHT_MULTIPLIER`皆標記【測試參數】，等9.7測試項目4校準出實際數字後再調整
- 目前唯一實際呼叫這個函式的地方是雙主修候選觸發的「特定科系伏筆權重」分支（用「創業線」權重當雙主修傾向的簡化代表，非文件明文規定的映射）；創業線/自由接案/海外線/演藝圈線/接手家業這五條伏筆線本身**沒有對應的敘事內容或下游數值後果**，設計文件9.6/9.7也承認這部分待補，本輪只把機率骨架建好，沒有發明新的敘事機制去填內容

**驗證**：語法檢查`JS_SYNTAX_OK`；Node固定測試值共132項全數✅，分四批：
- 55項單元測試（`test_university_system.js`）：科系分類/5年制判斷、轉系streak觸發、`resolveTransferOffer`高低數值下成功率確實隨知識值/自律程度變化（各50次試驗，高分組≥45次成功、低分組≤10次成功）、雙主修候選+zero-cost驗證、休學候選（健康/財務兩條線）、`sweepUniversityState`的冷卻/streak/onLeave自動復學、`advanceStageYear`的選科系觸發與畢業時雙主修自動結算
- 8項5科系/軌道混合壓力測試（`test_university_e2e.js`）：理工/醫藥5年/醫藥4年/商管/藝術五種組合各跑最多500回合的完整大學生涯，全程無crash、無NaN，回合數精確對上54×年數（216=4年、270=5年）
- 10項休學/肄業專項端到端測試（`test_university_withdrawal_e2e.js`）：刻意重摔健康值驗證觸發→肄業/休學兩條路徑，抓到並驗證修正了上述延畢額度用完仍可持續休學的漏洞，修正後6年制（4年+封頂2年延畢）精確跑出324回合畢業
- 32+27項回歸測試（沿用上一輪的`test_college_years.js`/`test_v21.js`）：確認本輪改動未影響既有的可變修業年限、興趣候選進度、回合預算等機制

**對應QA手冊**：34.2大學科系系統項目大部分移入34.1；9.7測試項目1-5、U-01~U-12測試矩陣仍待真實局數/樣本數測試（本輪只做固定值+壓力測試，不是U表格要求的30-50樣本統計）

**⚠️ 此改動會讓舊存檔跑不動**：新增十幾個角色欄位（多數有`||`預設值可向下相容，但`studentStatus`等關鍵欄位在舊存檔會是undefined，導致`advanceStageYear`等判斷失準），建議清空重來

**本輪已知的範圍縮減/簡化，誠實記錄**：
1. 轉系成功機率公式是Claude Code延伸設計，非文件明文規定
2. 雙主修中途主動放棄沒有獨立UI，只有「畢業時自動結算」這條退出路徑
3. 休學是「立即扣額度+固定回合數後自動復學」，不是真的暫停學期引擎
4. 學費仍是AI自由回報金額，跟9.9理想中「程式負責金額」有落差，本輪未處理
5. 五條下游伏筆線只有觸發機率骨架，沒有對應的敘事內容或後續數值影響
6. 發現但未修正：`exam_score_hint`payload計算受`studyCountThisTerm`提前歸零影響，讀書次數加成在考試當回合失效（見上方②）

## 2026-09-16（續13）——7.1.5死亡機制改版實作：機率公式+client端擲骰取代AI自由判斷

**〔開發部〕實作使用者提供的死亡機率公式提案，死亡機制從「AI自行判斷」全面改為「client端算好機率+擲骰決定」**
- 對應設計文件：七、7.1.5（2026-09-16新增機率公式提案，測試參數）
- 前情提要：7.1.5的機率公式先前一直卡在【待補充,下一輪討論】，文件明確要求公式定案前不動代碼，本輪使用者貼回完整提案（年齡基礎機率表+健康修正倍率+高風險加成，皆標記測試參數）

**做了什麼**：
- 檔案：`index.html`
  - 新增`DEATH_MIN_AGE=40`、`DEATH_AGE_BASE_PROBABILITY`（40-49到90+六個年齡帶）、`RISKY_LIFESTYLE_DEATH_BONUS=0.02`、`deathAgeBaseProbability()`/`deathHealthMultiplier()`/`computeDeathProbability()`（年齡基礎機率×健康修正倍率+高風險加成）、`rollAnnualDeathCheck()`（兩段式：第一次命中只埋前兆，已有前兆時第二次命中才真的觸發死亡，呼應既有定案7.1.2前兆機制）
  - `advanceStructuredTime()`的career分支：年份真的跳動時（`yearsToAdd>0`）才呼叫`rollAnnualDeathCheck()`，回傳`deathForeshadowNow`/`deathTriggered`兩個新欄位
  - `takeTurn()`：`forceEnding`從原本`state.age>=state.lifeExpectancy`（固定壽命值）改為`!!adv.deathTriggered`（機率判定結果）
  - `buildUserMessage()`：移除`accident_risk_hint`（AI自由判斷用的模糊提示），新增`death_foreshadow_now`（明確告知AI這回合要埋前兆，不再是AI自己判斷要不要埋）
  - system prompt：整段改寫，AI只負責在`forceEnding`/`death_foreshadow_now`已經決定好的框架內寫敘事，不再自行判斷要不要觸發死亡
  - JSON schema：移除`foreshadow_death`欄位（AI不再回報是否要埋前兆，這件事已經是client端決定好才告知AI）
  - `applyResult()`：移除`if(r.foreshadow_death)`的讀取邏輯、移除舊版`accidentalDeathValid`（19歲+已有前兆）防呆邏輯，改為`if(r.is_ending && forceEnding)`——AI完全無法自行發起死亡結局，只有client端決定`forceEnding`時才會生效
  - `newRoll()`/`succeedAsChild()`：移除`lifeExpectancy`（固定隨機70-88歲）的骰定與欄位，呼應文件「不再有壽命到達這條固定隨機值路徑」
  - debug面板：移除已不存在欄位「壽命」那一行
- 已用`node -e`對`<script>`內容做語法檢查，`JS_SYNTAX_OK`

**驗證**：Node固定測試值35項全數✅（`test_death_mechanism.js`）——公式各分段/健康倍率公式精確比對文件數字、40歲前機率恆為0、`rollAnnualDeathCheck`兩段式狀態機順序正確（用重複roll直到命中兩次驗證，因為即使最極端輸入機率也只有約39.5%,不會逼近100%無法用單次roll做決定性測試）、`advanceStructuredTime`只在真正跨年時才roll

**🔴 統計模擬驗證時發現一個提案概算沒考慮到的重要落差（已回寫進設計文件7.1.5，待內容組決定）**：
- 提案自己的「校準結果」章節（中位死亡年齡~79歲、90歲存活率~13%）是用「機率單次命中即視為死亡」的簡化模型算的
- 但7.1.2的前兆機制（既有定案，非本次新增）其實是兩段式關卡：第一次機率命中只埋前兆不會死，要再命中一次才真的觸發死亡
- 疊加這個既有關卡後，N=2000模擬跑出：中位死亡年齡**87歲**（差8歲）、90歲存活率**約41%**（提案概算13%的3倍多）、100歲存活率**約5%**（提案概算<1%的5倍多），**明顯超出提案自己在「測試組驗證重點1」設定的75-82歲通過區間**
- 對照組：用「單次命中即死」模型重跑同樣N=2000，結果（中位78歲、90歲存活率12-15%、100歲存活率0.6-1.1%）確實跟提案自述數字高度吻合，證實落差來源是前兆關卡疊加，不是我實作算錯或公式抄錯
- 測試組驗證重點2、3皆通過：健康15+高風險職業角色40多歲死亡率約2.3%（對照健康100+無風險組趨近0%，區辨力明顯）；80-89歲單年機率12%、90+單年機率25%，都遠低於「幾乎每年都死」的程度
- 需要內容組決定：(a)接受前兆關卡讓實際壽命比概算長，維持現有數字；(b)把年齡基礎機率表整體上修（概算約需1.3-1.5倍）讓實際分布落回目標區間；(c)重新設計前兆機制本身（例如已埋前兆後用提高後的機率），但這會動到7.1.2既有定案，需要另外討論

**對應QA手冊**：34.2大學科系系統項目之外新增死亡機制驗證記錄；死亡機制本體已完成並移入34.1

**⚠️ 此改動會讓舊存檔跑不動**：`lifeExpectancy`欄位整個移除，`s.lifeExpectancy`為undefined會讓舊存檔的`forceEnding`判斷完全失效（永遠不會用舊路徑觸發），建議清空重來

**本輪範圍**：只實作機率公式本身+client端擲骰決定觸發時機，沒有動7.1.1雙因子機制原本「健康越低機率越高、高風險行為加成」的既有精神（新公式已經內含這兩者，是合併後的版本）；沒有另外設計「已埋前兆後機率是否要提高」這類新規則（維持前兆前後用同一條機率曲線），這是上面提到的落差(c)選項如果要採用才需要額外設計的部分

## 2026-09-16（續14）——NPC關係卡顯示規則+戀愛系統v1實作

**〔整理+開發部〕寫入設計文件定案（4.1.1顯示規則、4.4戀愛系統）+ 實作`index.html`**
- 來源：使用者貼回claude.ai網頁版討論結論「NPC關係卡顯示 & 戀愛系統」交接文字
- 寫入定案前發現一個交接文字沒處理到的架構問題，已用AskUserQuestion請使用者拍板：4.3.1「第一次戀愛」(first_romance)里程碑原本是AI自由回報，新戀愛狀態機上線後要不要改成客戶端自動判斷——**使用者選擇「改為客戶端自動判定（推薦）」**，呼應4.3「AI自由判斷會不一致，一律交由客戶端結構化計算」既定精神
- 對應設計文件：新增四、4.1.1顯示規則、4.4戀愛系統（簡單版），4.3.1同步修改first_romance分類
- 設計文件異動已先貼回給使用者確認，快照存於`snapshots/life-sim-full-design-doc_2026-09-16k_romance.md`

**`index.html`實作內容**：
- 新增角色欄位（皆用`||`預設值處理，不需改`newRoll()`/`succeedAsChild()`push點，向下相容）：`c.romanceStatus`(null/ambiguous/dating/stable/breakup)、`c.romanceProgress`、`c.romanceStableStreak`、`c.romanceNegativeStreak`
- 新增常數（皆【測試參數,待驗證】，文件本身沒給精確門檻數字）：`ROMANCE_AMBIGUOUS_PROGRESS=3`、`ROMANCE_DATING_PROGRESS=9`（曖昧中再+6次）、`ROMANCE_STABLE_STREAK=6`、`ROMANCE_BREAKUP_STREAK=3`
- 新增`relationCategoryLabel()`（4.4：關係類別依狀態機顯示戀愛對象/曖昧對象，否則沿用原本身份文字）、`relationshipStatusLabel()`（4.1.1：目前關係狀態文字化，非戀愛關係的級距文字是實作判斷、非文件規定）、`applyRomanceSignal()`（狀態機本體：positive/negative推進progress/streak並轉換狀態，首次進入dating時自動標記`s.milestones.first_romance="completed"`）
- `demoteStaleCharacters()`（既有12回合降級機制）新增：`ambiguous`/`dating`狀態的NPC因閒置被降級為背景時，一併標記`romanceStatus="breakup"`（呼應4.4「分手沿用既有降級機制」定案）；`stable`狀態降級時保留stable不強制分手
- `applyResult()`的character_updates處理區塊：新增before/after snapshot（`beforeRelationship`）算出`relationshipHints`（方向性提示陣列，不精確報數字），呼叫`applyRomanceSignal()`；狀態轉換時優先顯示轉場提示（「你和X之間，好像開始有點曖昧了」等4種），否則顯示一般方向提示（「和X的關係更近了一些/有點疏遠了」）；`relationshipHints`存進log entry，比照`statChanges`同一套「先snapshot後diff」模式
- `rosterCard()`改版（4.1.1）：移除原本裸露0-100的`aff-bar`進度條，改用文字化的關係類別+關係狀態，新增`data-npc`屬性供點擊；新增`renderNpcDetailModal()`（點擊NPC卡叫出詳細頁：身份/關係類別/目前關係狀態/共同記憶/最近互動時間，關係值只作次要參考顯示），`bindPlaying()`新增`.rcard[data-npc]`點擊綁定
- CSS：移除`.aff-bar`/`.aff-fill`，新增`.status-text`，`.rcard`加`cursor:pointer`
- MILESTONE_DEFS：`first_romance`的`auto`從`false`改`true`
- JSON schema：`character_updates`新增`romantic_signal`欄位（AI只填positive/negative/null，分類用途）；system prompt新增對應說明（AI只分類，狀態機轉換不由AI判斷）；4.3事件狀態機制段落同步修改AI回報清單（5→4個，移除first_romance，改列入系統自動判斷的6個）
- `buildUserMessage()`的`active_characters`payload新增`romance_status`欄位，供AI參考語氣延續，不可自行變更
- Mock測試引擎：`character_updates`新增25%機率附帶`romantic_signal`（正向70%/負向30%，純測試比例）

**驗證**：Node vm載入`<script>`內容測試，共54項全數✅（三個測試檔存於scratchpad，非專案正式檔案）
- 單元測試（46項）：MILESTONE_DEFS的first_romance改為auto且不在AI可回報清單、`relationCategoryLabel`/`relationshipStatusLabel`各狀態文字正確、`applyRomanceSignal`完整狀態機（3次正向→曖昧中、累積9次→交往中、交往中連續6次正向→穩定交往、負向floor在0不會變負值、交往中/穩定交往連續3次負向→分手、負向streak會被中間插入的正向重置）、`demoteStaleCharacters`降級時曖昧中/交往中正確轉分手、stable維持不變、`applyResult()`完整pipeline（relationshipHints方向性提示不含精確數字、狀態轉換提示優先於一般提示、first_romance透過真實遊戲流程自動完成、AI試圖透過milestone_updates竄改first_romance被正確擋下）
- UI渲染測試（8項）：`rosterCard`不再顯示裸露數字、正確顯示關係類別/狀態文字、含data-npc屬性；`renderRelationshipHints`正確渲染/空陣列處理；`renderNpcDetailModal`正確組出詳細頁內容且呼叫`document.body.appendChild`
- 端到端壓力測試：20局×300回合mock模擬，全程無crash、無NaN、關係值维持在0-100範圍內；**發現一個mock測試工具本身的限制（非代碼bug）**：mock的character_updates每回合只30%機率隨機挑一位在場NPC互動，在多位NPC分散選中機率下，300回合內很少有單一NPC能自然累積到9次正向戀愛訊號進入「交往中」，只觀察到「曖昧中」自然出現，「交往中/穩定交往/分手」三個更深的狀態需要靠單元測試裡人為集中訊號到同一位NPC才驗證到（見上方單元測試），這反映的是mock隨機分散選角的測試工具特性，不代表真實AI（會延續同一段感情故事線）也會有這個問題，記錄供之後測試觀察參考

**已知範圍縮減，誠實記錄**：
1. 狀態轉換門檻數字（3/9/6/3）是Claude Code依「先簡單就好、之後再細修」的指示自訂的測試參數，設計文件本身標記待驗證，之後可能需要根據真實API測試調整
2. 「目前關係狀態」的非戀愛關係文字級距（多年好友/熟悉的朋友/普通朋友/認識不深/關係生疏）是實作時依關係值分段自訂，文件只給範例沒給精確級距，也是實作判斷
3. 變化提示的觸發時機（每次互動都跳 vs 累積到一定幅度才跳）本輪維持「每次有變化就跳」的最簡單版本，文件本身也標記【待補充,實作時決定】，之後如果覺得洗版可以再調整成累積門檻
4. 進階玩法（忠誠度、多線曖昧、劈腿風險、分手後主動選項）皆不在本次範圍，文件本身也明確排除

**⚠️ 此改動會讓舊存檔跑不動**：角色卡新增`romanceStatus`等欄位雖有`||`預設值可向下相容，但AI回應JSON schema變動（`character_updates`新增`romantic_signal`欄位、移除first_romance的AI回報資格）會讓舊版system prompt/schema假設跟新代碼不一致，建議清空重來
- 對應QA手冊：34.1新增本次項目、34.2移除（已完成）

## 2026-09-16（續15）——7.1.5死亡機率表分段修正（結案）

**〔整理+開發部〕寫入設計文件定案（7.1.5.1）+ 實作`index.html`的年齡基礎機率表 + Node複驗**
- 來源：使用者貼回內容組針對「7.1.5，9/16實作驗證，重要發現，待內容組決定是否調整」的正式決議——選(b)修正機率表數字，不選(a)/(c)
- 對應設計文件：七、7.1.5（機率公式段落）+ 新增7.1.5.1（修正發現與決策過程）；2.5.1一併結案「80+是否要抓過渡值」的既有待補充項

**設計文件異動**：
- 年齡基礎機率表70-79歲5%→7%、80-89歲12%→24%、90歲以後25%→32%，40-69歲三段不變
- 新增7.1.5.1記錄：為何70-79也要一起調（兩段式前兆關卡對機率本來就不低的年齡帶稀釋效果更大，只調80-89/90+會讓中位死亡年齡卡在83歲下不去）、為何90+也一起調（避免跟80-89打平削弱「避免高齡機率停滯」的設計初衷，且對中位死亡年齡/90歲存活率完全沒影響）、驗證方法（獨立模型N=100,000重現初版87歲/40.6%/5.3%→網格搜尋→N=200,000大樣本確認）
- 快照存於`snapshots/life-sim-full-design-doc_2026-09-16l_death_v2.md`

**`index.html`實作**：
- `DEATH_AGE_BASE_PROBABILITY`表：70-79歲`0.05→0.07`、80-89歲`0.12→0.24`、90+`0.25→0.32`，40-69三段不變；健康修正倍率/高風險加成/40歲門檻/前兆機制皆未變動

**驗證（不是憑印象估算，直接用`index.html`實際引擎函式跑模擬）**：
- 用Node vm載入`<script>`內容，直接呼叫`computeDeathProbability()`/`rollAnnualDeathCheck()`（非另外重寫一份邏輯），N=200,000模擬（健康50/無高風險基準角色）：中位死亡年齡**82歲**（目標75-82歲✓）、25/75百分位80/86歲、90歲存活率**12.69%**（目標貼近13%✓）、100歲存活率**0.52%**（目標<1%✓）——與提案文件的模型結果（82歲/12.7%/0.5%）高度吻合，證實`index.html`的真實引擎行為跟提案用來校準的獨立模型一致
- 重點2複驗：健康15+高風險角色40-49歲死亡率**1.98%**（提案估算約2.2%，同量級），對照健康100+無風險組**0.01%**，區辨力明顯保留
- 重點3：80-89歲單年機率24.0%、90+單年機率32.0%，與表格定義一致，遠低於「幾乎每年都死」
- 新增檢查點：68-92歲逐歲死亡分布直方圖，70/80/90歲年齡帶邊界處因機率表本身跳躍（相鄰年齡帶機率相差超過3倍）出現自然階梯，但**每一歲分布本身平滑遞增/遞減，沒有異常斷層或整數年齡群聚**，階梯是機率表十年一段設計的預期結果，不是bug
- **額外發現，已回寫進設計文件2.5.1**：驗證時確認回合預算(`roundsPerYearForAge()`)與死亡機率判定(`rollAnnualDeathCheck()`)完全解耦——後者固定「每滿一個實際年齡才roll一次」，不受該年齡帶回合數多寡影響。這解答了提案原本的疑慮（模型未納入「80+每年5回合」這類回合制細節，擔心數字需要再微調）：回合數多寡不影響這個機率模型，可以放心採用，不需要再往回調；同時順帶結案2.5.1原本「80+是否要跟70-79抓過渡值」的待補充項（兩系統互不依賴，不需要過渡值）
- 已用`node -e`對`<script>`內容做語法檢查，`JS_SYNTAX_OK`

**結論**：7.1.5（含新增的7.1.5.1）正式結案，不再是【待內容組決定是否調整】的未決事項。對應QA手冊：34.2移除此項（已解決），34.1新增本次項目，完整驗證數字記錄於`qa/QA_34附錄_歷史封存.md`

**⚠️ 存檔相容性**：本次只改動`DEATH_AGE_BASE_PROBABILITY`表格內的數字，沒有新增/移除欄位、沒有改變資料結構，舊存檔可以正常繼續使用，不需要清空重來

## 2026-09-16（續16）——大學系統補充四項定案：伏筆權重封測數字+雙主修中途放棄+休學真正暫停學期引擎+學費改家長支付

**〔整理+開發部〕寫入設計文件定案（9.3/9.5.2/9.5.3/3.4.4/9.9）+ 實作`index.html`**
- 來源：使用者貼回claude.ai網頁版討論結論「大學系統補充討論——定案草稿」，四項全數【定案】
- 對應設計文件：九、9.3/9.5.2/9.5.3、三、3.4.4、9.9責任邊界表同步修改
- 設計文件異動已先寫入並存快照`snapshots/life-sim-full-design-doc_2026-09-16m_university3.md`

**`index.html`實作**：
1. **伏筆權重封測數字**：`FORESHADOW_BASE_RATE`從測試用的0.05改為定案的0.08；`FORESHADOW_WEIGHT_MULTIPLIER`(高2.0/中1.0/低0.4)本來就跟定案數字一致，只更新標記從【測試參數】改為【定案,標記測試參數】
2. **雙主修中途放棄**：新增`checkDualMajorAbandonCandidate()`（`dual_major_status=active`期間自律或財務健康度連續3回合<40分成立候選，冷卻4回合，掛進`sweepUniversityState()`）、`renderDualMajorAbandonModal()`/`resolveDualMajorAbandonOffer()`（撐下去/正式撤銷二選一，撤銷後`dualMajorStatus="abandoned"`為終局狀態，`checkDualMajorCandidate()`既有的`|| s.dualMajorStatus`守門會自動擋下重新申請，不需額外程式碼）
3. **休學真正暫停學期引擎**：`advanceStructuredTime()`新增`mode==="college" && s.leaveStatus==="onLeave"`早期分支，完全跳過`SEMESTER_PHASES`/`YEAR_SEGMENTS`引擎（不動`segmentIndex`/`turnsInSegment`、不判斷考試）；`resolveWithdrawalOffer()`的"leave"分支新增記錄`leaveSemesterHalfIndex`（休學當下在上學期或下學期，供復學時重置用）；`sweepUniversityState()`復學時（`leaveResumeCountdown`歸零）改為把`segmentIndex`重置回休學前那個學期的開頭（不是中斷點）、`turnsInSegment`/`studyCountThisTerm`歸零、`yearInStage`維持不變；`computeTimeLabel()`新增休學中顯示「（科系年級）・休學中」取代原本仍顯示段落標籤的行為；`LEAVE_OF_ABSENCE_TURNS`從4改為7（6-8回合區間，比照2.3假期段落份量）；休學次數上限沿用既有`COLLEGE_DELAY_CAP=2`共用額度機制，不新增第二層限制（已有UI disable+程式層雙重把關）
4. **學費改家長支付**：system prompt的`one_time_transaction`說明移除學費/雙主修學分費範例，新增明確指示「學費統一由家長支付，不從玩家現金扣款，AI不可透過one_time_transaction/expense_change回報任何學費金額，僅能當背景細節描寫」；大學系統prompt區塊同步更新`university_recent_event`範例（新增`dual_major_abandoned`/`leave_resumed`類型）與休學中的敘事方向指示（不出現考試/選課情節，改走生活/打工/家庭/自我探索）；雙主修候選彈窗文案移除「學費都會增加」措辭

**驗證**：Node vm測試共39+15局混合端到端模擬全數✅
- 39項單元測試（`test_university3.js`）：伏筆權重高16%/中8%/低3.2%精確吻合且排序正確；雙主修中途放棄狀態機完整路徑（selfDiscipline驅動/financialHealth驅動兩條觸發路徑、未達門檻不觸發、冷卻期間不累積streak、"keep"/"abandon"兩種決定、abandoned後無法重新申請）；休學引擎凍結（`leaveSemesterHalfIndex`正確記錄上/下學期、休學期間`advanceStructuredTime()`確認`segmentIndex`/`turnsInSegment`/`yearInStage`完全不動且`examTriggered`恆false、`computeTimeLabel()`正確顯示休學中、復學時正確重置回該學期開頭而非中斷點、4次休學耗盡2年額度且第5次被正確擋下）
- 15局×最多600回合端到端混合模擬（選科系/上課/打工/隨機觸發休學/雙主修候選/雙主修中途放棄/肄業/畢業全路徑）：全程無crash、無NaN，六種關鍵事件（休學/復學/雙主修候選/雙主修中途放棄/肄業/畢業）全數在模擬中真實觸發過
- 已用`node -e`對`<script>`內容做語法檢查，`JS_SYNTAX_OK`

**🔴 發現一個既有函式的量級落差（非本次新增bug，這次沿用同一函式才暴露出來，已記錄不在本輪範圍內修正）**：`computeFinancialHealth()`的門檻是校準給真實新台幣（40000/100000/3000000量級），但學生時期`s.cash`/`s.monthlyIncome`是遊戲內單位（30/60/120量級，見3.4.10）。實測典型學生數值（30/60/120）餵進`computeFinancialHealth()`，結果恆為**0分**——代表雙主修中途放棄候選在學生階段的「財務健康度<40」這條腿幾乎必然恆成立，selfDiscipline判別力被蓋掉。這個函式同時也是`financial_health`payload欄位/`can_afford_medical`（3.4.5醫療費用分級）共用的底層邏輯，代表這個落差可能早就存在於學生時期的醫療費用分級判斷，只是這次才暴露。已記入QA手冊34.3，供下一輪決定如何處理（學生時期改用另一套換算，或函式本身依輸入量級判斷）

**對應QA手冊**：34.2的「大學科系系統：雙主修中途主動放棄」「休學期間真正暫停學期引擎」「學費結構化計算」三項移入34.1（學費項目因需求本身改變——不再需要結構化計算，等同結案）；「8大科系權重絕對機率校準」改記為「第二階段」，仍在34.2追蹤（base rate/multiplier已定案，但五條伏筆線敘事內容/判定時機仍待補）；新增34.3的computeFinancialHealth量級落差追蹤項

**⚠️ 存檔相容性**：新增角色欄位（`dualMajorAbandonStreak`/`dualMajorAbandonCooldown`/`leaveSemesterHalfIndex`）皆用`||`預設值處理，不需初始化即可運作；`LEAVE_OF_ABSENCE_TURNS`數值變動只影響休學中的角色體感時長，不影響資料結構；`advanceStructuredTime()`新增的onLeave分支只在`leaveStatus==="onLeave"`時生效，非休學角色行為完全不變。整體對舊存檔友善，不強制清空重來，但正在休學中的舊存檔角色下次推進時會改走新的凍結邏輯（之前若有部分段落進度會被reset，屬於本次規則改版的預期行為）

## 2026-09-16（續17）——介面新增現階段進度指示 + 【測試第三版基準點】

**〔開發部〕新增年齡/現階段細顆粒時間標籤 + 本段落進度條**
- 使用者需求：畫面目前只有粗顆粒的「XX歲・找方向期」，希望能看到更細的「現在在學期/年度裡的哪個進度」，方便掌握節奏
- 檔案：`index.html`
  - 新增`computeStageProgress(s)`：依目前`timeState.stageMode`回傳這個「小段落」的進度`{label, current, total}`——學生時期(非休學)是目前學期段落(開學初/準備期/考試/假期等)的`turnsInSegment/budget`；休學中是休學倒數`(LEAVE_OF_ABSENCE_TURNS-leaveResumeCountdown)/LEAVE_OF_ABSENCE_TURNS`；出社會(career)是`adultTurnsInYear/roundsPerYearForAge(age)`
  - 新增`renderProgressStrip(s)`，在`.cover`（原本只顯示年齡+粗顆粒人生階段的圓形戳章）與`.ledger`（財富/數值列表）之間插入一條新的進度列：細顆粒時間標籤（沿用既有`computeTimeLabel()`，例如「大二・期中準備期」「入職第3年」「35歲」「大三・休學中」）+ 這個段落跑了幾回合的文字與進度條
  - CSS新增`.progress-strip`/`.progress-track`/`.progress-fill`
- 驗證：Node固定測試值13項全數✅——三種模式(highschool/college/career)+休學中的進度計算正確、`computeTimeLabel()`各種標籤正確帶出、進度條寬度百分比計算正確、完整`renderPlaying()`不崩潰且含新的`progress-strip`區塊
- 已用`node -e`對`<script>`內容做語法檢查，`JS_SYNTAX_OK`
- **⚠️ 純Node邏輯驗證，尚未在瀏覽器裡目視確認排版/樣式**——比照本專案一貫做法（自由輸入字數限制等純UI呈現項目向來都是「邏輯用Node測、實際點選畫面由使用者自測」），這次的CSS排版是否好看、進度條在手機寬度下會不會跑版，需要使用者實際開瀏覽器看一次
- 本輪不影響存檔結構，純呈現層新增，不需要清空重來

**〔整理〕定調測試第三版（Test v3）**
- 使用者表示這個介面進度指示做完就要正式下去試玩，比照先前「測試第二版」基準點慣例，記錄這個時間點作為Test v3基準
- 自Test v2（續7）之後，v3新增/改版內容：7.1.5死亡機率表分段修正（結案）、NPC關係卡顯示規則(4.1.1)+戀愛系統v1(4.4)、大學系統補充四項（伏筆權重封測數字/雙主修中途放棄/休學真正暫停學期引擎/學費改家長支付）、本次的現階段進度指示
- 完整「試玩前檢查清單」已整理成一份獨立訊息回覆給使用者，涵蓋：已知bug、量級落差、未實作/範圍縮減項目、存檔相容性、費用控制提醒；QA手冊34節現況即為這份清單的底稿，供之後回顧核對

## 2026-09-16（續18）——修正兩個已知問題：exam_score_hint執行順序bug + 學生時期財務健康度規則缺口

**〔開發部〕使用者對前一輪回報的兩個bug給出結論後修正**
- 來源：使用者讀過共同基準文件與設計文件對應段落後，明確區分兩個問題的性質——第一個純屬執行順序bug不涉及規則；第二個是真正的規則缺口，需要決策並補進設計文件
- 對應設計文件：二、2.4.1（不需異動，純代碼bug）、三、3.4.6（補充定案）

**Bug 1：exam_score_hint執行順序修正**
- 診斷：使用者指出2.4.1本來就定案清楚「exam_score_hint要用本次備考的讀書次數計算，讀書次數則是累積到考試觸發時才歸零」，正確順序應該是「先用累積值算hint，再歸零」，現在的實作把順序寫反了——單純是Claude Code的執行順序bug，不需要另外定案
- 修正：`buildUserMessage()`的`exam_score_hint`/`study_count_this_term`兩個欄位，在`exam_triggered`為true時改讀`timeCtx.studyCountAtExam`（`takeTurn()`裡本來就有存的考前快照，續12為了轉系候選判定而加的，這次直接重用），不再直接讀`s.studyCountThisTerm`（考試觸發當下已經被`advanceStructuredTime()`歸零）
- 驗證：Node固定測試值確認考前讀書5次、知識值50時，hint正確算出65（50+5×3），且非考試回合不受影響仍讀即時計數

**Bug 2：學生時期財務健康度改用家庭真實NT$數字（3.4.6補充定案）**
- 使用者決策：學生時期呼叫財務健康度時，輸入源改為**家庭**月收入/存款（3.4.1表格前兩欄真實NT$數字，依家境三級距代入既有公式），不使用玩家個人零用錢（3.4.1已定案為遊戲內單位、不對應真實幣值）；22歲出社會後改回算玩家個人數字
- 理由：3.4.1本來就說家庭月收入/存款兩欄「維持NT$真實金額，不受(9/14)改版影響」；學費已由家長支付，學生財務壓力語意上就該反映「家裡撐不撐得住」；維持3.4.6「一套公式打天下」的精神，不用另外發明學生專屬分級表
- 檔案：`index.html`
  - 新增`FAMILY_FINANCE_BY_TIER`（清寒/小康/富裕三級距真實NT$月收入/存款範圍，取自3.4.1表格；富裕級距上限文件未定案，標記【測試參數】延伸合理範圍）、`rollFamilyFinance(tier)`
  - `rollEconomicTier()`/`rollEconomicTierForced()`（分別供`newRoll()`/`succeedAsChild()`使用）回傳值新增`familyMonthlyIncome`/`familySavings`；`newRoll()`的政治世家分支也另外骰一次富裕級距的家庭數字
  - 新增`computeFinancialHealthForState(s)`包裝函式：`!s.occupationCategory`（尚未出社會）時用`s.familySavings`/`s.familyMonthlyIncome`，已出社會則沿用原本`computeFinancialHealth(s.cash, s.monthlyIncome)`
  - 6個原本直接呼叫`computeFinancialHealth(s.cash, s.monthlyIncome)`的地方（`buildUserMessage`的financial_health欄位、雙主修中途放棄候選判定、4個死亡結局核心/次要條件）全部改用`computeFinancialHealthForState(s)`
- 驗證：Node固定測試值23項全數✅——清寒/小康/富裕三種家境開局，財務健康度不再恆為0，且落在文件粗略級距（20-30/50-70/85-100）附近；對照組驗證舊寫法（餵學生個人零用錢）確實恆為0分，證實修正前後差異；已出社會角色正確改回算玩家個人數字（刻意設超高家庭數字驗證不會被誤用）；富裕家境+高自律學生的雙主修放棄候選不再被財務健康度這條腿強制觸發
- 已用`node -e`語法檢查`JS_SYNTAX_OK`，並重跑8局全生涯端到端回歸（含今天新增的戀愛系統/大學系統/死亡機制/進度指示全部疊在一起）確認無crash、無NaN

**已知,非bug的副作用**：清寒家庭學生因為財務健康度區間本身就落在20-30分（低於雙主修放棄候選的40分門檻），這條腿會比小康/富裕家庭更容易持續成立——這是符合「家境差的學生財務壓力確實比較大」的敘事邏輯，不是修正前那種「恆為0導致判別力失能」的狀態

**對應QA手冊**：34.3的兩項🔴既有bug移入34.1，完整驗證細節記錄於`qa/QA_34附錄_歷史封存.md`

**⚠️ 存檔相容性**：新增角色欄位`familyMonthlyIncome`/`familySavings`皆有`||0`預設值處理（雖然正常流程一定會在`newRoll()`/`succeedAsChild()`時賦值），舊存檔沒有這兩個欄位時財務健康度會暫時算出0分（等同修正前的行為），不會crash；建議下次重新開局讓新角色正確帶有這兩個欄位，不強制立即清空重來但也不特別提供沿用舊存檔的保證

## 2026-09-16（續19）——切換至真實API測試（使用者明確授權）

**〔整理〕`USE_MOCK`改為`false`**
- 使用者提供PDF截圖（`人生草稿.pdf`），顯示mock模式的旁白高度重複（例如「容翻了翻手邊的課本，窗外的蟬聲比什麼都吵…」連續出現3次），確認mock模式的固定文字庫無法反映真實敘事品質，需要真實API才能實際評估
- 已用AskUserQuestion依CLAUDE.md費用控制鐵律明確詢問並取得授權（每次都要重新問，不沿用先前批准）
- 檔案：`index.html`，`USE_MOCK`從`true`改為`false`，`WORKER_URL`沿用既有Cloudflare Worker中繼站設定，未變動
- 已用`node -e`語法檢查`JS_SYNTAX_OK`
- 接下來每回合都會實際呼叫真實API、產生費用，測試結束或告一段落後應提醒使用者是否要切回`USE_MOCK=true`

## 2026-09-17——重大bug修正：存讀檔機制從未在一般瀏覽器運作過 + 真實API 403排查

**〔開發部〕修正`window.storage`不存在的問題**
- 來源：使用者實際用真實API試玩時，貼了瀏覽器DevTools Console截圖，發現除了API 403錯誤外，還有一個每回合都在發生的`TypeError: Cannot read properties of undefined (reading 'set')`（`saveGame (index.html:399:29)`）
- **診斷**：`loadGame()`/`saveGame()`/`hardReset()`原本呼叫的是`window.storage.get/set/delete`，這不是任何一般瀏覽器（Chrome/Safari/Firefox等）內建的API，一般瀏覽器裡`window.storage`是`undefined`，呼叫`.get`/`.set`/`.delete`必定拋出TypeError。因為外層有`try/catch`把錯誤吞掉只做`console.warn`，遊戲不會白屏崩潰、可以正常繼續玩，所以這個bug一直沒被發現——**代表無論是本機直接開檔案(`file://`)還是正式部署到Cloudflare Pages，存讀檔功能實際上從未真正運作過**，玩家清空重整頁面就會遺失進度，只是因為遊戲全程都在記憶體裡的`state`變數運作，不影響單次遊玩過程本身
- 修正：`index.html`三處呼叫全部改用瀏覽器標準的`localStorage`（`getItem`/`setItem`/`removeItem`），拿掉不存在的`window.storage`依賴
- 驗證：Node測試6項全數✅（用一個真的會記住資料的假localStorage物件，而非只回傳null的假物件，實際驗證存檔內容能正確寫入/讀出/清空），並確認`node -e`語法檢查`JS_SYNTAX_OK`
- **附帶發現**：今天稍早所有Node vm測試腳本裡的`localStorageStub`其實從來沒被真正呼叫到（因為代碼呼叫的是`window.storage`不是`localStorage`），這些測試腳本本身沒有問題（它們驗證的邏輯跟存讀檔無關），但值得記錄：這代表`window.storage`這個bug存在期間，純Node測試也测不出來，只有真人在真瀏覽器裡點才會發現，凸顯「瀏覽器點選由使用者自測」這個既有分工在這次抓bug上的必要性

**〔整理〕真實API 403「Request not allowed」排查**
- 同一份截圖顯示`callAI()`呼叫`WORKER_URL`(`https://life-game.smile80275.workers.dev/`)收到HTTP 403，訊息為「Request not allowed」
- 已排除的可能性：`MODEL`常數`claude-sonnet-4-6`經查是有效的模型ID（上一代Sonnet，非幻覺字串），不是403的原因；純粹是版本較舊、價格比目前最新的`claude-sonnet-5`貴，是否要順便升級留給使用者決定，不影響本次403排查
- **判斷**：Worker本身很可能對請求來源(Origin/Referer)做白名單檢查，只允許正式部署網域呼叫，本機直接以`file://`開啟時瀏覽器送出的來源不被信任，才會被Worker主動拒絕（同一份console截圖也出現「'file:' URLs are treated as unique security origins」的瀏覽器警告，方向吻合）。Worker原始碼不在本專案資料夾內，無法直接排查/修改
- 已臨時起一個本機HTTP伺服器（`python3 -m http.server 8765`）讓使用者改用`http://localhost:8765/index.html`測試，排除`file://`來源這個變因，交由使用者實測後回報結果，再決定是否需要使用者調整Worker白名單設定或改到正式Cloudflare Pages網址測試

**⚠️ 存檔相容性**：`localStorage`存的`STORAGE_KEY`跟原本`window.storage`從未真正寫入過，等同全新開始，沒有「舊存檔」需要相容的問題

## 2026-09-18～19——真實API除錯：模型權限403 + 截斷 + AI偶爾不回JSON，改用structured outputs根治

**〔開發部〕真實API測試過程中連續排查三個問題，逐一定位並修正**

使用者切到真實API後持續回報錯誤並提供console截圖，逐輪排查如下：

1. **403「Request not allowed」**：先用curl直接對Worker發診斷請求排除了Origin白名單/CORS/請求大小等假設（`access-control-allow-origin: *`，小請求跟大請求都能正常轉發到Anthropic並收到真正的Anthropic格式錯誤），確認Worker本身沒有擋，真正原因是`MODEL`常數`claude-sonnet-4-6`這組API金鑰沒有存取權限（403=權限問題，不是404 not_found=模型不存在，兩者有明確區別）。改用`claude-sonnet-5`（同時也更新更便宜：$2/$10 vs 原$3/$15）後403消失
2. **回應沒有文字內容/JSON被截斷**：Sonnet 5預設會自動開啟adaptive thinking，會佔用`max_tokens`額度，原本的`max_tokens:1400`太小，思考沒想完或文字寫到一半就被截斷。已將`max_tokens`調高到3000，並加上`output_config:{effort:"low"}`降低思考的token消耗（敘事生成不需要高強度推理）
3. **AI偶爾直接寫故事、完全不包JSON**：改善(1)(2)後仍偶爾出現`stop_reason=end_turn`（正常結束、內容完整）但整個回應是純文字故事而非JSON的情況——這是「用system prompt文字指示AI自己包JSON」這個做法本身的可靠度上限，純文字指示本來就有機率被忽略。**根治做法**：改用Anthropic API的structured outputs機制（`output_config.format`），在API層面強制回應一定符合指定JSON結構，不再是「拜託AI自己包好」

**檔案**：`index.html`
- 新增`RESPONSE_JSON_SCHEMA`常數：完整對照原本system prompt文字描述的JSON模板，逐欄位轉成正式JSON Schema（每個物件都有`required`/`additionalProperties:false`，nullable欄位用`type:["string","null"]`等寫法）
- `callAI()`的請求body新增`output_config.format:{type:"json_schema",schema:RESPONSE_JSON_SCHEMA}`，與原本已加的`effort:"low"`一起放在`output_config`底下
- 為了排查過程本身，也順手把兩處錯誤訊息的診斷內容從只顯示前200字加大到2000字，並在「沒有文字內容」「JSON解析失敗」兩處新增顯示`stop_reason`/`usage`（能直接看出是否卡在max_tokens、thinking用了多少token），這次除錯能這麼快定位問題很大程度是靠這兩個诊断欄位

**驗證**：
- Node測試28項✅：確認`RESPONSE_JSON_SCHEMA`每個物件節點的`required`都跟`properties`完全對應、`additionalProperties`皆為false、且跟system prompt原本文字模板的欄位一一比對無缺漏無多餘、JSON.stringify/parse往返正常
- 已用`node -e`對`<script>`內容做語法檢查，`JS_SYNTAX_OK`
- **structured outputs本身沒有另外花錢驗證**——這是API請求格式的改動，透過使用者接下來在瀏覽器裡的真實操作驗證即可，不需要我這邊另外發起呼叫

**排查方法論記錄（供之後類似狀況參考）**：診斷403時，沒有直接改代碼亂試，而是先用curl對Worker發送「刻意不完整/無效的請求」（例如缺model欄位、或用假的model名稱）——這類請求會被Anthropic在最前面就擋下拒絕，不會真的觸發計費，但拒絕訊息的格式（400/404 vs 403）就足以排除或確認假設，是一種安全的除錯手段

**⚠️ 存檔相容性**：純API請求格式/錯誤訊息改動，不影響任何遊戲狀態資料結構，不影響存檔

## 2026-09-19（續）——新增「回憶錄」畫面，解決玩家覺得舊日記消失的困擾

**〔開發部〕純UI功能：完整回顧歷史，不影響AI實際記憶範圍**
- 使用者反映玩了十回合後感覺前面內容被清空；討論後選定「中等」方案——先做一個讓玩家自己能翻閱完整歷史的回憶錄畫面，理由是成本低、立即解決本次反映的困擾、之後如果要做分層記憶庫也不衝突
- 明確範圍：這**只解決「玩家覺得東西消失了」的體驗問題**，AI實際能讀到的上下文範圍完全沒變（`recent_log`仍是最近2則原文、`chronicle_recent`仍是最近6則人生履歷摘要），AI的長期敘事記憶能力沒有跟著變好
- 檔案：`index.html`
  - `s.log`上限從60大幅提高到2000（足夠涵蓋單一人生全程約1200-1400回合不流失；`newRoll()`/`succeedAsChild()`每次轉世都會把log重置為`[]`，不會跨世累積，不用擔心無限增長）
  - 新增`renderMemoirModal()`：比照既有modal-backdrop彈窗模式，把`state.log`全部攤開顯示（沿用既有`renderParagraphs`/`renderStatChanges`/`renderRelationshipHints`渲染邏輯，跟主畫面日記呈現方式一致），彈窗放大到560px/92vw並限制最高82vh、內部可滾動，避免上千則內容把彈窗撐爆
  - `footer-actions`新增「📖 回憶錄（N則）」連結，點擊叫出彈窗；`bindPlaying()`新增對應綁定
- 驗證：Node測試4項＋內容比對2項全數✅——log上限2000正確運作（超過時捨棄最舊的，不是捨棄最新的）、新角色/轉世正確重置為空陣列、彈窗正確顯示全部10則測試內容（不是只顯示最近6則）、正確顯示「N則」計數；額外確認`recent_log`/`chronicle_recent`兩處AI payload欄位程式碼完全沒被動到
- 已用`node -e`對`<script>`內容做語法檢查，`JS_SYNTAX_OK`

**⚠️ 存檔相容性**：純UI功能+一個數字上限調整，不影響現有存檔資料結構，不需要清空重來

## 2026-09-19（續2）——記憶系統定案實作：NPC認識來源欄位 + 履歷判準（轉職/生育/父母退休/重大事件上限）

**〔開發部〕實作「人生草稿・記憶系統 定案討論結論（合併版）」P1項目**
- 對應設計文件：四、4.1（新增認識時間/來源欄位）、4.2.2（新增,履歷判準）
- 寫入定案前，P1項目「定義哪些事件值得進永久人生履歷的判準」交接文字本身只標記缺口、沒給實際判準內容，已用AskUserQuestion詢問，使用者選擇自己先分析給出結論（4.3.1已涵蓋的部分直接沿用「清單非封閉」擴充；「重大失敗/成就」這類無結構化訊號的，採用者提出的「十年後回看」測試題+呼應3.2.2 milestone精神的每階段2次上限）——這是使用者自己的分析結論，不是Claude Code發明判準

**檔案**：`index.html`

1. **NPC「認識時間/來源」欄位**（4.1）
   - 新角色物件新增`origin`欄位；`newRoll()`4個家人建立點（一般父母/隔代教養/服刑家長/手足）都給固定的「家人，從出生起」類描述；`succeedAsChild()`的3個延續點（新父母×2、手足）同步補上，並確認是**從新主角視角重新寫**，不沿用上一世「怎麼認識」的舊故事
   - AI透過`new_characters`建立新角色卡時要求一併提供`origin`（簡短交代怎麼認識、大概何時）；`applyResult()`的`new_characters`處理讀取`nc.origin`存進角色物件
   - `renderNpcDetailModal()`（4.1.1既有詳細頁）新增顯示這個欄位

2. **履歷判準（4.2.2）**
   - `MILESTONE_DEFS`新增3項，沿用4.3.1「清單非封閉」原則：`first_job_change`(第一次轉職，自動——非首次`job_change`觸發)、`first_child`(生育或領養，自動——`new_characters`的`is_child:true`觸發)、`parent_retirement`(父母退休，AI回報，無結構化訊號)
   - 新增`shouldRecordJudgedMajorEvent(s, majorEventType)`：呼應3.2.2 milestone防重複精神，「achievement」「setback」類型的`major_event_summary`每人生階段(`timeState.stageMode`)由程式計數，上限2次，超過整筆不記錄進履歷；其餘類型（既有里程碑等）不受影響
   - JSON schema新增`major_event_type`（achievement/setback/null，AI只分類不算次數）；`new_characters`項目新增必填的`origin`
   - system prompt同步更新：`new_characters`新增origin說明、`major_event_summary`新增「十年後回看」判準說明+`major_event_type`分類說明、4.3事件狀態機制段落更新里程碑數量與清單（10→13個，AI回報4→5個，自動判斷6→8個）
   - Mock測試引擎同步更新：`new_characters`附帶隨機`origin`、`major_event_summary`約30%機率分類成achievement/setback供測試上限機制

**驗證**：Node vm測試54項全數✅
- Schema一致性：`new_characters.items`含`origin`且列入required、`major_event_type`列入頂層required、整份schema逐節點required/additionalProperties再次交叉驗證（沿用9/19稍早那份檢查邏輯）
- `MILESTONE_DEFS`新增3項且auto/AI回報分類正確、清單長度13
- `origin`欄位：newRoll()初始家人全數非空、AI提供的origin正確存進角色卡
- `first_child`/`first_job_change`自動判定：生育觸發正確、第一份工作只觸發`first_full_time_job`不觸發`first_job_change`、第二份工作才觸發`first_job_change`
- 世代傳承：新父母/手足的`origin`都正確被設定（不是沿用undefined或上一世舊值）
- `shouldRecordJudgedMajorEvent`完整狀態機：null類型不受上限、achievement/setback共用同一個階段計數器且上限2次、超過後正確擋下、切換人生階段後計數器重新歸零
- 完整`applyResult()`整合：2次judged事件正常記錄、第3次同階段judged事件正確被擋下(chronicle長度不變)、非judged的major_event_summary不受影響正常記錄
- NPC詳細頁正確顯示origin文字
- 額外重跑8局全生涯端到端回歸（含今天新增的所有系統），全程無crash、無NaN

已用`node -e`對`<script>`內容做語法檢查，`JS_SYNTAX_OK`（過程中抓到一次自己的編輯失誤：改mock版`new_characters.push()`加`origin`欄位時漏掉了收尾的`});`，語法檢查當場抓到並修正）

**⚠️ 存檔相容性**：新增的`origin`/`majorEventJudgedCountByStage`欄位、3個新里程碑id，對舊存檔而言會是undefined/未初始化，不會crash（讀取時皆有防呆），但舊角色卡不會有回溯的認識來源資訊，新建立的角色卡才會有；建議重新開局讓新角色完整套用

## 2026-09-19（續3）——修正「重要共同記憶」既有定案未落實：改為機制性上限2則FIFO

**〔開發部〕修正NPC角色卡summary欄位無上限累加問題**
- 來源：使用者問「NPC欄位是否會將近期重要事件也寫入」時，Claude Code順帶發現`character_updates`的`summary_add`原本是無上限字串累加（每次都用「；」接在舊字串後面，不會捨棄），違反4.1原文「1-2句話等級」的既有定案，也跟同日新增的4.2.1「L4不做AI長期全文記憶」精神矛盾
- 使用者明確判定：這是既有規則未落實，必須修正，不適用「無實測案例先暫緩」的判斷邏輯；並給出完整方案比較與定案文字（採FIFO上限2則，不採「直接取代」，理由是取代會讓長期關係的單次瑣事蓋掉真正重要的記憶；真正的重大事件本來就由4.2人生履歷永久承接，不依賴這個欄位）
- 對應設計文件：四、4.1「重要共同記憶」欄位補充定案

**檔案**：`index.html`
- 新增`appendNpcSharedMemory(c, addition)`：內部維護`c.summaryEntries`陣列（機制性上限2則，FIFO捨棄最舊），`c.summary`永遠是`summaryEntries.join("；")`的衍生值，供既有讀取點（payload/NPC詳細頁/世代傳承）原樣繼續使用，不需要改動其他讀取邏輯
  - 相容既有存檔：舊角色的`c.summary`可能已經是很長的無上限累加字串，第一次呼叫時整段視為單一則（不嘗試用「；」拆解，避免內容本身剛好含「；」字元誤判斷句），之後幾次更新會自然被FIFO擠出去
- `applyResult()`的`character_updates`處理，`summary_add`邏輯改呼叫`appendNpcSharedMemory()`取代原本直接字串拼接
- system prompt新增一句：summary_add只保留最近2則，AI不用擔心長度或該不該精簡

**驗證**：Node測試14項全數✅——基本FIFO行為（第3則正確擠掉第1則、之後持續正確輪替）、空值/null不觸發更新、舊存檔相容性（長字串視為單一則、經過兩次更新後正確被完全擠出）、完整`applyResult()`端到端整合驗證FIFO確實生效。已用`node -e`語法檢查`JS_SYNTAX_OK`，並重跑8局全生涯端到端回歸確認無crash無NaN

**⚠️ 存檔相容性**：新增`summaryEntries`欄位對舊存檔是undefined，但`appendNpcSharedMemory()`有防呆會自動從既有`c.summary`初始化，不會crash，舊角色的長字串會在後續1-2次更新內自然收斂到新的2則上限，不需要手動處理

## 2026-09-19（續4）——修正structured outputs schema：nullable欄位改用anyOf取代type陣列

**〔開發部〕修正9/19新增的structured outputs導致400錯誤**
- 來源：使用者提供console截圖，顯示`output_config.format.schema: Invalid schema: Enum value 'ordinary' does not match declared type ['string', 'null']`，HTTP 400
- 診斷：查`claude-api`技能的`shared/tool-use-concepts.md`「Structured Outputs」章節的JSON Schema支援清單，確認Anthropic structured outputs**不支援`type`寫成陣列**（例如`["string","null"]`）來表示nullable欄位，支援清單裡明確列出`enum`/`const`/`anyOf`/`allOf`/`$ref`/`$def`，但type本身只接受單一基本型別。前一輪實作的`RESPONSE_JSON_SCHEMA`大量使用`type:["string","null"]`/`type:["object","null"]`模式，這是根本性的schema寫法錯誤，不是只有`event_type`一個欄位的問題——只是它剛好是第一個觸發驗證失敗的欄位
- 檔案：`index.html`，`RESPONSE_JSON_SCHEMA`全面改寫，11個nullable欄位（`event_type`、`event_id`、`job_change`、`housing_choice`、`interest_event`、`college_location_choice`、`major_event_summary`、`major_event_type`、`ending_title`、`ending_summary`、`character_updates`裡的`romantic_signal`）全部從`type:[...]`改成`anyOf:[{實際型別...},{type:"null"}]`

**驗證**：Node vm測試共195項全數✅
- 新增`test_schema_anyof.js`（128項）：遞迴走訪整份schema（含anyOf分支），確認全部節點`type`都不是陣列、所有物件節點的`required`仍精確對應`properties`且`additionalProperties:false`、11個nullable欄位全部改用anyOf且都有null分支、`event_type`的anyOf字串分支enum值正確、跟system prompt文字模板逐欄位交叉比對仍完全一致
- 重跑9/19稍早的既有測試套件（memory system 48項+2個bugfix 23項+FIFO 14項）全數✅，確認schema重構沒有影響其他既有邏輯
- 重跑8局全生涯端到端回歸，無crash無NaN
- 已用`node -e`對`<script>`內容做語法檢查，`JS_SYNTAX_OK`

**⚠️ 這是純API請求格式修正，不影響任何遊戲狀態資料結構，不影響存檔**

## 2026-09-19（續5）——修正structured outputs第二個400：schema過大（compiled grammar too large）

**〔開發部〕拿掉schema裡所有enum約束，縮小compiled grammar大小**
- 來源：使用者提供console截圖，改用anyOf後400錯誤變成不同訊息：`The compiled grammar is too large, which would cause performance issues. Simplify your tool schemas or reduce the number of strict tools.`
- 診斷：`RESPONSE_JSON_SCHEMA`本身結構偏大（25個頂層欄位、多層巢狀物件、8處`enum`約束，其中`interest_event.category`是7個中文詞的enum），Anthropic把JSON schema編譯成限制輸出的grammar，這份schema編譯後超出大小限制
- 檔案：`index.html`，拿掉全部8處`enum`約束（`event_type`/`expense_change.action`/`housing_choice.type`/`interest_event.category`/`interest_event.reaction`/`milestone_updates.status`/`character_updates.romantic_signal`/`major_event_type`），只保留`type`結構本身；enum移除不影響正確性——這些欄位的實際值域本來就由system prompt文字指示AI遵守，且代碼讀取這些值時原本就有防呆（`===`比對/`||`預設值/`switch`找不到就當null處理），不是依賴schema強制enum才能正常運作。schema整體JSON大小從移除前量測約3933 bytes（連同結構本身，非單純enum差異，精確的移除前大小當下未留存，僅供參考現在的量級）
- 這次沒有把結構本身簡化掉（頂層25欄位、巢狀物件層數都維持不變），只拿掉enum——如果這次還是「grammar太大」，代表問題出在結構複雜度本身，下一步才需要考慮更激進的做法（例如放棄structured outputs、退回純文字指示JSON+9/18已經調過的max_tokens/effort當防線）

**驗證**：Node vm測試共195項全數✅（重跑9/19全部既有測試套件：schema anyOf 128項含「schema內零enum約束」新檢查、memory system 48項、bugfix 23項、FIFO 14項，皆不受enum移除影響），額外重跑8局全生涯端到端回歸無crash無NaN。已用`node -e`語法檢查`JS_SYNTAX_OK`

**⚠️ 純API請求格式修正，不影響任何遊戲狀態資料結構，不影響存檔**

## 2026-09-19（續6）——撤回structured outputs，退回純文字指示JSON

**〔開發部〕使用者決策：拿掉enum後仍是「compiled grammar too large」400，直接撤回**
- 來源：使用者提供第4輪console截圖，確認拿掉全部8個enum後錯誤訊息完全沒變，還是`The compiled grammar is too large`
- 判斷：連續4輪真實API失敗（403模型權限→anyOf格式錯誤→grammar太大→拿掉enum仍太大），enum移除對「grammar太大」這個特定錯誤完全沒有影響，代表問題是schema本身的規模/複雜度（25個頂層欄位+多層巢狀物件+多個陣列型欄位），不知道確切的編譯限制在哪，继续猜測縮小結構風險高、可能還要再花好幾輪真實API測試才能試出可行的規模。已用AskUserQuestion請使用者決定，選擇撤回，不再嘗試縮小schema
- 檔案：`index.html`
  - 整段刪除`RESPONSE_JSON_SCHEMA`常數（131行，含9/19當天新增的origin/major_event_type欄位定義），不留作死代碼
  - `callAI()`請求body的`output_config`拿掉`format`欄位，只留`effort:"low"`；新增註解記錄嘗試過structured outputs但因schema過大放棄的完整脈絡，供之後參考
  - 退回依賴system prompt既有的「只回傳一個JSON物件…JSON結構如下」純文字指示，搭配9/18已調整的`max_tokens:3000`（避免思考吃光額度被截斷）與`effort:"low"`（降低思考token消耗）當防線，加上既有的失敗重試一次機制

**驗證**：Node vm測試共63項全數✅（重跑9/19全部既有測試套件，拿掉`test_memory_system.js`裡已不適用的schema一致性檢查28項，其餘26項遊戲邏輯驗證+2個bugfix測試23項+FIFO測試14項皆不受影響，確認origin欄位/里程碑自動判定/major_event_type上限機制等實際遊戲邏輯完全不依賴structured outputs，撤回不影響任何功能正確性），額外重跑8局全生涯端到端回歸無crash無NaN。已用`node -e`語法檢查`JS_SYNTAX_OK`

**已知取捨，誠實記錄**：純文字指示JSON無法百分之百保證AI一定會照格式回應（先前實測中出現過1次AI整段寫成故事、完全不包JSON的情況），這是接受的已知風險，不是治本方案；如果之後這個失敗模式頻繁出現，才需要重新考慮structured outputs（屆時要先想清楚怎麼在有限的schema複雜度預算內取捨要保留哪些欄位的嚴格型別驗證），或考慮把回應拆成多次較小的API呼叫降低單次schema負擔

**⚠️ 純API請求格式修正，不影響任何遊戲狀態資料結構，不影響存檔**

## 2026-09-19（續7）——Prompt caching + 4.2.1.1短期上下文壓縮規則（取代recent_log）

**〔開發部〕實作兩項降低成本/控制長度的改動**

**1. Prompt caching**
- 來源：使用者追問「一定要三萬字都每回合讀取嗎」，檢查後確認`buildSystemPrompt()`完全是固定文字（無參數、無任何`${}`插值），每個玩家每一回合送出去的system prompt一字不差，卻完全沒開快取，等於每次都用全額input token價格重複付這3萬多字
- 檔案：`index.html`，`callAI()`的`system`欄位從純字串改成`[{type:"text", text:buildSystemPrompt(), cache_control:{type:"ephemeral"}}]`，這是Anthropic prompt caching的標準寫法
- 這個修法**只影響計費方式，不影響AI實際讀到的內容**，不解決AI偶爾不遵守JSON格式的問題（那是另一個獨立的、已知會殘留的機率性風險，見續6記錄）

**2. 4.2.1.1短期上下文壓縮規則**
- 對應設計文件：四、4.2.1.1（新增小節）
- 來源：使用者追問system prompt長度時，claude.ai Project盤點發現「日常瑣碎回合的短期上下文」是設計文件既有空白（4.2履歷/4.1 NPC記憶都只處理長期壓縮），定案：保留最近3回合完整原文，第4回合起自動壓縮成1-2句摘要、永久保留、與4.2履歷分工並行。摘要生成歸屬與AI可見範圍數字明確授權由Claude Code決定
- 檔案：`index.html`
  - 新增`RECENT_FULL_TEXT_TURNS=3`（定案數字）、`RECENT_SUMMARY_TURNS=6`（Claude Code判斷,呼應L3履歷同樣抓6則）
  - JSON schema/system prompt新增**每回合必填**的`turn_summary`欄位（1-2句話摘要這回合），AI在正常敘事生成的同時順手產出，不需要額外呼叫API做摘要——這是回應交接文字「摘要由誰產生」這個待決定事項的實作選擇，理由是另外呼叫API做摘要會讓每回合變成兩次呼叫，成本與延遲都加倍
  - `applyResult()`的`s.log.push()`新增儲存`turnSummary`
  - `buildUserMessage()`拿掉原本的`recent_log`（只給最近2則原文、更早的完全看不到），改成`recent_turns_full`（最近3回合完整原文）+`recent_turns_summary`（再往前6則的`turnSummary`壓縮摘要，缺少時退回原文避免舊存檔資料出現空字串），更早的完全不送
  - Mock測試引擎同步新增`turn_summary`模擬輸出（一般回合用narrative前20字當簡易摘要，death結局回合用固定摘要）

**驗證**：Node vm測試共19項新增全數✅（攔截fetch驗證`system`欄位真的是`[{type:"text",...,cache_control:...}]`陣列結構而非字串、`output_config.format`確認保持移除狀態、`recent_turns_full`精確對應最近3回合原文、`recent_turns_summary`精確對應再往前6則的摘要且順序正確、超出視窗的更早回合確認完全不在payload內、遊戲剛開始（總回合數不足3+6）不會crash、舊資料缺少turnSummary時正確退回原文），重跑9/19全部既有測試套件（memory system 26項+bugfix 23項+FIFO 14項）確認無連帶影響，額外重跑8局全生涯端到端回歸無crash無NaN。已用`node -e`語法檢查`JS_SYNTAX_OK`

**⚠️ 存檔相容性**：`s.log`條目新增`turnSummary`欄位，舊存檔裡的舊條目沒有這個欄位時`buildUserMessage()`已有防呆退回原文，不會crash；但因為AI回應payload結構變動（`recent_log`欄位整個移除），建議清空重來避免混淆

## 2026-09-19（續8）——存檔跨裝置機制：封測階段純金鑰模式

**〔開發部〕實作十、10.1封測階段：純金鑰模式**
- 對應設計文件：十、存檔與帳號系統，10.1（10.2帳號系統暫緩，之後帶做，本次不實作）
- 來源：使用者貼claude.ai網頁版定案文件「【定案】存檔跨裝置機制」，寫入設計文件章節時把既有第十章「內容範例庫」機械式整章重編號為第十一章（ToC＋全文所有`10.x`標題與交叉引用），過程見對話紀錄，用Node腳本逐一比對「預期出現次數」驗證，抓到並修正一處因括號位置不同少算一次的引用（`見十、10.1.5`其中一處沒有外層括號）
- 跨裝置存檔需要伺服器端儲存，本地端沒有Worker原始碼也沒有部署權限：向使用者說明後，使用者選擇提供一組唯讀範圍（僅`Workers Scripts:Read`）的Cloudflare API Token，我用它讀取現有Worker原始碼（純代理AI請求的簡單版本），沒有、也不會用這組Token做任何寫入/部署動作——比照專案一直以來「Pages也是手動上傳」的慣例，改好的`worker.js`交給使用者自己貼回後台部署
- 檔案：`worker/worker.js`（新增資料夾與檔案，本地保存，尚未部署）
  - 保留原本AI代理行為完全不變（任何非`/save`、`/slots`、`/load`的POST請求都當作AI代理轉發到`https://api.anthropic.com/v1/messages`）
  - 新增三支API：`POST /save`（存檔，驗證key/slot/state格式，1MB上限防呆）、`GET /slots`（回傳某金鑰底下3個slot的meta摘要，不含完整state）、`GET /load`（回傳指定slot完整state）
  - 新增KV binding需求（`env.SAVES`），部署前使用者需自行在Cloudflare後台新增KV Namespace綁定，檔案開頭已寫好中文部署步驟註解
- 檔案：`index.html`
  - 新增`RECOVERY_KEY_STORAGE_NAME`/`ACTIVE_SLOT_STORAGE_NAME`/`MAX_LIFE_SLOTS=3`常數、`generateRecoveryKey()`（10 bytes隨機數，格式化成`XXXX-XXXX-XXXX-XXXX-XX`大寫十六進位）
  - 原本的`loadGame()`改寫為`initApp()`：本機無金鑰→顯示`keyChoice`（開始新旅程／我有復原金鑰）；本機有金鑰+有記錄使用中的slot→呼叫`tryLoadSlot()`自動載入（雲端優先，失敗退回本機快取）；本機有金鑰但沒有使用中slot→呼叫`showSlotPicker()`顯示3個slot卡片供選擇
  - 新增`renderKeyChoice`/`renderKeyReveal`（顯示金鑰＋明確「請務必記下，遺失無法救回」警語）/`renderKeyInput`（輸入既有金鑰）/`renderSlotPicker`（3張卡片，已佔用顯示`第N段人生：姓名・年齡歲・階段`，空白顯示可點擊開始新人生）4組render+bind函式，刪除已被取代的舊`renderSetupIntro`/`bindSetupIntro`死代碼
  - `saveGame()`改寫：本機快取一律先寫（依`slot`分開存不同key），再視情況（有金鑰+有slot+`phase`為`playing`或`ending`）額外打`POST /save`同步雲端，雲端失敗不影響本機正常運作（`console.warn`吞掉錯誤）
  - `hardReset()`改寫：只清除目前使用中slot的本機快取，不動復原金鑰本身；沒有使用中slot時退回`keyChoice`
  - 新增「🔑 我的復原金鑰」連結（`renderPlaying()`footer-actions／`bindPlaying()`），`renderKeyViewModal()`用modal顯示目前快取的金鑰，方便玩家隨時複查
  - 檔案最底部bootstrap呼叫從`loadGame();`改成`initApp();`

**驗證**：
- Worker端新增`test_worker.mjs`，25項全數✅（OPTIONS CORS放行、原AI代理行為不變含header注入、`/save`成功路徑＋4種驗證錯誤情境、`/slots`混合佔用/空白slot＋未知金鑰回傳全null非404＋確認不洩漏完整state只給meta、`/load`成功/空slot 404/無效key或slot 400、同slot重複存檔正確覆蓋），用mock KV（Map模擬）與mock`global.fetch`攔截，未打到真實網路
- 客戶端新增`test_key_slot_system.js`，31項全數✅（首次造訪金鑰生成與開始新旅程流程、輸入既有金鑰後正確渲染混合佔用/空白的slot picker、已記錄使用中slot時自動載入成功、雲端讀取失敗正確退回本機快取not爆炸、`saveGame()`正確只在playing/ending階段觸發雲端同步且payload格式正確、`hardReset()`正確只清除當前slot不影響金鑰本身、金鑰檢視modal正確顯示、點選空白slot正確直接進identity不會誤打`/load`）
- 重跑9/19全部既有測試套件確認無連帶影響：`test_memory_system.js`26項✅、`test_two_bugfixes.js`23項✅、`test_summary_fifo.js`14項✅、`test_caching_and_shortcontext.js`19項✅，額外重跑8局全生涯端到端回歸無crash（8局全部走完，8死亡，最長483回合，涵蓋highschool/college/career/college:onLeave全部stage mode）。已用`node -e`語法檢查`JS_SYNTAX_OK`

**⚠️ 尚未部署，需要使用者手動完成才能實際運作**：
1. 把`worker/worker.js`內容貼到Cloudflare後台Worker編輯器取代舊內容並Deploy
2. **新增KV Namespace綁定**（Settings → Bindings → Add binding → KV Namespace，變數名稱`SAVES`，沒有現成namespace的話同一畫面可直接新建，例如命名`life-game-saves`）——這是`/save`、`/slots`、`/load`三支API能正常運作的硬性前提，沒做這步會導致`env.SAVES`是undefined直接丟錯
3. 確認既有的`ANTHROPIC_API_KEY`這個Secret沒有被動到，還在
4. 部署完成後提醒使用者撤銷/刪除這次為了讀取原始碼而建立的唯讀Cloudflare API Token（讀取原始碼用不到常駐的權限）

**⚠️ 存檔相容性**：`loadGame()`→`initApp()`重寫＋新增金鑰/slot前置流程，舊版沒有金鑰概念的存檔直接對不上新架構，建議清空重來

## 2026-09-19（續9）——高張力抉擇機率判定：明牌檢定機制（首版白名單：期末考成績公布）

**〔開發部〕實作十一、11.1-11.4明牌檢定機制**
- 對應設計文件：十一、高張力抉擇機率判定——明牌檢定機制（11.1設計精神/11.2呈現格式/11.3觸發機制:白名單制/11.4白名單首版範圍）
- 來源：使用者貼claude.ai網頁版討論結論，參考遊戲「硃砂痣」的呈現方式（命運待擲→明示要判定但機率保密→點擊觸發→判定過程公開），定案在特定高張力情境開一個例外窗口，與現有「敘事包裹一切」隱性判定精神並存
- 定案文字只規定呈現格式要有「骰值＋屬性加成換算依據＋門檻對比＋成敗結果」，沒有給具體骰值範圍/及格門檻數字：**以下數字皆為實作階段自行選定的【測試參數，待驗證】**，60分及格門檻沿用台灣常見及格線慣例、骰值範圍(1-100，90%機率落在-10~+10、10%機率意外落在±11~25且正負對半)呼應system prompt既有「hint±10分左右合理，保留少量機率更意外」的敘事指示，改寫成client端可明牌呈現的擲骰版本——這幾個具體數字之後如需校準，屬正常測試調校範圍
- 檔案：`index.html`
  - 新增`EXAM_CHECK_PASS_THRESHOLD=60`、`rollFinalExamCheck(wisdom, studyCount)`：沿用既有2.4.1大方向層公式算出`base`，擲1-100骰值決定swing（90%落在-10~10，10%落在±11~25對半），`finalScore=clamp(base+swing,0,100)`，`passed=finalScore>=60`
  - `takeTurn()`：`advanceStructuredTime()`判定`examType==="期末考"`時，呼叫AI之前就先擲骰決定確切分數（呼應7.1.5死亡機制精神，不讓AI自由裁量這個數字），期中考不受影響、維持原本hint範圍由AI自由發揮；`timeCtx`新增`examCheck`欄位
  - `buildUserMessage()`：新增`exam_final_score`欄位（期末考明牌檢定後的確切分數），`exam_score_hint`改為只在「有考試但不是明牌檢定範圍」（即期中考）時才提供，兩者互斥；system prompt指示同步更新，告知AI若`exam_final_score`不是null必須直接採用這個確切數字寫敘事，不能自己改成別的分數
  - 新增`renderExamCheckModal(check)`：兩階段揭曉彈窗（先顯示「命運已經決定了，要現在揭曉判定過程嗎？」，點擊「揭曉」後才顯示完整的骰值/屬性加成換算/門檻對比/成敗結果明細），彈窗放進`takeTurn()`既有的pending modal cascade
- 設計文件更新過程：新章節「十一、高張力抉擇機率判定——明牌檢定機制」插入後，原「十一、內容範例庫」機械式順延為「十二、內容範例庫」，全文交叉引用（ToC/5.2.3/5.1.5×3/9.1「號碼待補」/9.5.2「11.3.2」×2/附錄章節本體所有子章節編號）用Python腳本逐一比對出現次數驗證後同步更新，已存快照`snapshots/life-sim-full-design-doc_2026-09-19e_revealcheck.md`

**驗證**：Node vm測試新增`test_exam_check.js`共29項全數✅——`rollFinalExamCheck()`純函式（基準公式clamp正確、極端值0/100兩端clamp、finalScore/roll範圍、passThreshold=60、passed邏輯、5000次模擬驗證90%/10%機率分布皆落在預期範圍內且意外區間確實被跑到）；`buildUserMessage()`欄位邏輯（期末考明牌檢定：exam_final_score帶確切分數且exam_score_hint為null；期中考：維持原本hint行為且exam_final_score為null；非考試回合：兩者皆null）；`takeTurn()`完整流程串接（監控/覆寫`advanceStructuredTime()`強制期末考觸發，驗證modal正確appendChild、兩階段揭曉的顯示/隱藏切換正確、不重複彈出；期中考觸發與非考試回合皆確認不會誤跳出此modal）。重跑9/19當天全部既有測試套件（memory system 26項+bugfix 23項+FIFO 14項+caching/短期上下文19項+worker 25項+key/slot 31項）確認無連帶影響，額外重跑8局全生涯端到端回歸無crash無NaN。已用`node -e`語法檢查`JS_SYNTAX_OK`

**⚠️ 存檔相容性**：`timeCtx`新增`examCheck`欄位、payload新增`exam_final_score`欄位，皆為新增欄位不影響既有存檔結構本身，不需要清空重來；但期末考的AI敘事行為改變（分數不再由AI自由生成），舊存檔繼續玩不受影響

## 2026-09-19（續10）——選項生成技巧規則定案並實作（一、1.2.7）

**〔開發部〕實作1.2.7選項生成技巧規則，改寫system prompt的choices欄位指示**
- 對應設計文件：一、1.2.7選項生成技巧規則（1.2.7.1動作具體化/1.2.7.2可內嵌動作或台詞/1.2.7.3可帶態度立場/1.2.7.4字數放寬20-35字/1.2.7.5濃度分級/1.2.7.6中立性紅線）
- 來源：使用者上一輪先貼了全部標記【草稿】的討論結論，本輪確認定案，並確認1.2.7.4原文「那個規格保留給1.2.7.6的高張力節點」是筆誤，已修正為「1.2.7.5」（實際描述高張力節點濃度的內容在1.2.7.5濃度分級，不是1.2.7.6中立性紅線）——設計文件已同步修正並把全部【草稿】升級為【定案】
- 檔案：`index.html`，改寫system prompt裡choices欄位的指示文字（取代原本「給3到4個簡短（不超過12個字）、彼此方向不同的具體行動選項」的舊版精簡指示），新指示涵蓋：動作具體化取代抽象意圖（附範例）、可用「(動作)+一句話」結構、同一件事的選項可在姿態/立場上做差異（呼應依附兩軸人格光譜，不強制對應固定角色設定）、字數上限放寬到約20-35字、濃度依情境分級（日常瑣事維持精簡但至少一個具體動作/道具細節；情感線/關鍵抉擇/`exam_final_score`不是null的明牌檢定節點可以寫到接近硃砂痣濃度）、中立性紅線（同組選項畫面豐富度必須對等，加細節方向只能是畫面/立場不能暗示結果好壞）
- 純屬system prompt文字調整，沒有新增/修改任何JSON schema欄位、沒有client端邏輯分支，choices按鈕的CSS（`display:block;width:100%`）本來就會自然換行，不需要另外調整樣式因應字數放寬

**驗證**：Node vm測試新增`test_option_style.js`8項全數✅（確認system prompt正確引用1.2.7選項生成技巧規則、包含20-35字新範圍、不再含舊版「不超過12個字」文字、包含動作具體化範例、濃度分級/中立性紅線關鍵字、`exam_final_score`作為高張力節點觸發依據的引用、維持3-4個方向不同選項的既有要求）。重跑9/19當天全部既有測試套件（memory 26+bugfix 23+FIFO 14+caching/短期上下文19+exam check 29+key/slot 31）確認無連帶影響，額外重跑8局全生涯端到端回歸無crash無NaN。已用`node -e`語法檢查`JS_SYNTAX_OK`

**已知限制，誠實記錄**：這次只驗證了system prompt文字本身正確寫入且沒有破壞其他既有指示，**AI是否真的會照這套規則寫出更有畫面感、姿態差異化、且中立性不被破壞的選項，需要真實API測試才能驗證**（純文字風格指示，沒有客觀對錯可用Node固定測試值驗證），屬於這次改動的已知邊界，非代碼問題

**⚠️ 純system prompt文字調整，不影響任何遊戲狀態資料結構，不影響存檔**

## 2026-09-19（續11）——改用強制工具呼叫取代純文字指示JSON，修正真實API偶發「AI整段寫成故事」失敗

**〔開發部〕真實API測試發現連續兩回合「AI整段寫故事、完全不包JSON」，改用tool use架構解決**
- 來源：使用者提供console截圖，`JSON解析失敗：Unexpected token '寒'...is not valid JSON`，確認AI這次的原始回應直接以純文字故事開頭，完全沒有JSON外殼，且連續兩回合都中招（比續6記錄的「目前只出現過1次」頻率更高）
- 決策過程：先提出「拆成兩次API呼叫」（敘事一次、結構化抽取一次）方案，使用者反問「我玩別款文字遊戲都不會這樣，為什麼」，促成重新研究——查證後發現業界標準做法是**工具呼叫（tool use/function calling）**，不是「請AI在文字裡寫JSON」這種純提示詞做法；且工具呼叫與先前放棄的`output_config.format`(structured outputs)是兩套不同機制，後者才會經過有大小上限的「編譯grammar」，只要不額外加`strict:true`，一般工具呼叫不受那個限制影響，維持一次API呼叫即可，不需要拆成兩次（費用/延遲都不會翻倍）
- 檔案：`index.html`
  - 新增`TURN_RESULT_TOOL`常數：把原本system prompt結尾的JSON結構範例，轉換成正式的`input_schema`（27個欄位對齊原本的JSON模板，故意不加`strict:true`、也不加enum，避開跟structured outputs同一套grammar編譯限制）
  - `callAI()`請求body新增`tools:[TURN_RESULT_TOOL]`與`tool_choice:{type:"tool",name:"submit_turn_result"}`，強制AI只能透過呼叫這個工具回應，架構上排除「忘記包JSON」的可能性
  - `callAI()`回應解析從「找text區塊→trim→去markdown code fence→JSON.parse」整段改成「找tool_use區塊→直接讀.input」——API端已經幫忙解析成物件，不再需要手動JSON.parse，`Unexpected token`這類parse錯誤在架構上不會再發生
  - system prompt結尾的「只回傳一個JSON物件...JSON結構如下：{...}」整段（約30行）改成一句話指示改用`submit_turn_result`工具，各欄位的遊戲邏輯規則說明（才識/財務/死亡機制等）完全沒有變動，只改變「怎麼把資料交出來」這個機制本身
  - Worker端(`worker/worker.js`)不需要改動——原本就是透明轉發POST body給Anthropic API，新增的`tools`/`tool_choice`欄位會自動一併轉發過去

**驗證**：Node vm測試更新`test_caching_and_shortcontext.js`（fetch stub改回傳tool_use區塊而非text區塊，新增7項檢查：`tools`陣列正確宣告/`tool_choice`正確強制指定/工具沒有`strict:true`/`callAI()`回傳值直接是tool input物件不用再parse/schema欄位跟`applyResult()`實際讀取的27個欄位完全對齊、無遺漏無多餘），`test_exam_check.js`的fetch stub同步更新為tool_use格式。全部重跑9/19當天既有測試套件：memory 26+bugfix 23+FIFO 14+caching/短期上下文27+exam check 29+option style 8+worker 25+key/slot 31，全數✅，確認無連帶影響。額外重跑8局全生涯端到端回歸無crash無NaN。已用`node -e`語法檢查`JS_SYNTAX_OK`

**已知限制，誠實記錄**：這次修正解決的是「AI完全不回JSON」這個特定失敗模式，架構上已排除；但沒有加`strict:true`，代表工具參數本身仍然沒有100%的schema型別保證（理論上仍可能出現型別跟schema描述不符的情況，例如該給數字卻給了字串）——不過現有`applyResult()`本來就對幾乎每個欄位都有`||`防呆預設值，單一欄位格式異常不會讓整回合報廢，跟修正前「整段parse失敗、整回合被容錯機制吃掉」比是明確的可靠度提升。**這個修正本身尚未經過真實API驗證**，是否真的解決了實測中看到的失敗頻率，需要下次同意花費用測試真實API時才能確認

**⚠️ 純API請求/解析機制修正，不影響任何遊戲狀態資料結構，不影響存檔**

## 2026-09-19（續12）——修正tool use新失敗模式：AI把工具呼叫寫成舊式偽XML文字混進narrative

**〔開發部〕真實API測試發現改用強制工具呼叫後出現的新失敗模式，補上偵測+重試防護**
- 來源：使用者連續兩回合真實API測試貼出畫面，遊戲有正常推進到下一回合，但顯示的敘事文字結尾混進了`</narrative>`、`<parameter name="turn_summary">`這類標籤碎片，後面接著整串`<parameter name="age_advance">0</parameter>`等欄位標籤
- 診斷：這是AI把整個回應寫成類似Anthropic早期「prompted tool use」文件範例的偽XML格式（`<parameter name="...">值</parameter>`），而不是老實把值分別填進submit_turn_result工具schema的各自獨立欄位——但AI確實有呼叫這個工具（`toolBlock`存在），只是`toolBlock.input.narrative`這個欄位的字串內容本身被汙染，導致續11新增的「找不到tool_use就throw」防護完全擋不住這個情況，續11的架構把這種汙染資料當成功悄悄吃下去，比修正前「直接JSON.parse失敗、觸發重試」還要更隱蔽
- 檔案：`index.html`
  - 新增`isMalformedTurnResult(input)`：偵測narrative欄位是否缺漏/空白/混入`</narrative>`或`<parameter name=`這類標籤片段、turn_summary是否缺漏/空白、choices是否不是陣列
  - `callAI()`在確認`toolBlock`存在之後，新增這道驗證，偵測到汙染就`throw`，觸發`takeTurn()`既有的「重試一次，仍失敗才顯示中性容錯敘事」機制，不再讓汙染資料悄悄過關

**驗證**：Node vm測試新增`test_malformed_tool_detect.js`14項全數✅——直接用使用者實測貼出的兩段真實汙染樣本文字驗證`isMalformedTurnResult()`正確判定為malformed；各種缺漏/型別錯誤情境（narrative缺漏/空字串、turn_summary缺漏、choices不是陣列、input本身是null）皆正確判定；乾淨正常資料與死亡結局的空choices陣列皆確認不會被誤判；`callAI()`整合測試確認汙染回應會throw（不會悄悄回傳）、乾淨回應正常回傳且欄位內容正確。重跑9/19當天全部既有測試套件（memory 26+bugfix 23+FIFO 14+caching/短期上下文27+exam check 29+option style 8+worker 25+key/slot 31）全數✅，額外重跑8局全生涯端到端回歸無crash無NaN。已用`node -e`語法檢查`JS_SYNTAX_OK`

**已知限制，誠實記錄**：這道防護只能「偵測到就重試」，不能保證重試後AI一定不會再犯同樣的錯誤；根本原因（為什麼強制工具呼叫下AI有時會退化成寫舊式偽XML文字）目前只在真實測試中觀察到現象，沒有查到明確的觸發條件或官方說明，不確定跟schema規模（27個欄位）、`effort:"low"`設定是否有關。如果重試後仍常出現這個失敗模式，下一步可以考慮提高`effort`到`medium`（有額外費用）或進一步縮小工具schema，但這兩個方向都還沒有實測驗證，暫不執行

**⚠️ 純API回應驗證邏輯修正，不影響任何遊戲狀態資料結構，不影響存檔**

## 2026-09-19（續13）——強化偵測範圍：污染欄位不限narrative、非結局回合選項不該是空陣列

**〔開發部〕真實API測試發現續12的偵測漏抓的情況，補強`isMalformedTurnResult()`**
- 來源：使用者貼出截圖回報「選項消失了」——遊戲畫面上方還看得到一則舊回合殘留的`<parameter name="event_type">`等標籤碎片，下方新回合觸發了容錯機制（顯示「旁白剛剛恍神了一下」），但選項按鈕整組消失、只剩自訂輸入欄位可用
- 診斷：兩個問題疊加——(1) 續12的`isMalformedTurnResult()`只檢查`narrative`欄位裡有沒有標籤碎片，但污染有時出現在其他欄位（例如`character_updates`/`event_type`/`interest_event`），narrative本身乾淨就會被誤判為合法，讓半污染的回應被`applyResult()`接受；(2) 那次半污染回應的`choices`欄位因此也連帶空掉或缺漏（`s.choices = r.choices||[]`存進`[]`），下一回合`snapshotForUndo()`把這個已經是空陣列的`choices`存進快照，若這次呼叫又失敗，`applySnapshot()`就會把空陣列的選項還原回來，畫面上選項因此整組消失
- 檔案：`index.html`，改寫`isMalformedTurnResult()`：
  1. 不再只檢查`narrative`欄位，改成序列化整個`input`物件、對全部欄位做一次性掃描，涵蓋污染出現在任何欄位的情況
  2. 新增規則：非結局回合（`is_ending`不是true）的`choices`不能是空陣列，呼應一、1.2.7「給3到4個選項」的既有定案——只有真正的死亡結局回合才允許`choices`為空

**驗證**：`test_malformed_tool_detect.js`新增2項檢查（共16項全數✅）：非結局回合choices空陣列正確判定為malformed；污染出現在narrative以外的欄位（測試用character_updates模擬）也正確被抓到；原本死亡結局場景choices空陣列不誤判的測試同步補上明確的`is_ending:true`。同步修正`test_exam_check.js`裡一個舊測資的`choices:[]`（原本非結局場景卻給空陣列，在新規則下會誤觸發，已改成非空陣列）。重跑9/19當天全部既有測試套件（memory 26+bugfix 23+FIFO 14+caching/短期上下文27+exam check 29+option style 8+worker 25+key/slot 31）全數✅，額外重跑8局全生涯端到端回歸無crash無NaN。已用`node -e`語法檢查`JS_SYNTAX_OK`

**⚠️ 純API回應驗證邏輯修正，不影響任何遊戲狀態資料結構，不影響存檔**。**已知限制**：這次修正無法回溯修復使用者瀏覽器裡已經卡進`state.choices`的舊污染資料（那個存檔的`choices`欄位已經是空陣列），但玩家仍可透過自訂輸入欄位正常繼續遊戲，下一次成功的回合會自然覆蓋回正常的選項陣列

## 2026-09-19（續14）——3.4財富系統全面改為遊戲幣單位，取代NT$具體金額制

**〔開發部〕財富系統貨幣單位改版，讀設計文件3.4改版內容後實作**
- 對應設計文件：三、3.4總覽、3.4.1、3.4.2、3.4.3、3.4.4、3.4.6、3.4.7、3.4.9，六、6.1（1078行閾值bug）
- 做了什麼：
  - `FAMILY_FINANCE_BY_TIER`（家庭月收入/存款三級距）、`computeFinancialHealth()`內部NT$斷點常數、`economicTierForSavingsAmount()`（重生用經濟分級）三處統一÷1000，避免財務健康度/家境分級恆算出接近0分或永遠判清寒（重現9/16修過的量級不對bug，這次範圍擴大到22歲後整個人生）
  - `LIVING_COST_BASE`（基本生活開銷低/中/高基準）同步÷1000（4000/8000/14000→4/8/14）——這組是code-only測試參數，設計文件3.4.8本身未定案具體金額，但正職薪資改成個位數~三位數後不跟著縮小會直接讓所有人入不敷出，屬於必要的連帶修正
  - `reincarnate()`裡轉生存款加成`savingsCarry`從5000改為5——這是9/14學生零用錢改遊戲幣單位制時漏改的殘留NT$量級數字，這次一併抓出來修正
  - system prompt（`buildSystemPrompt()`）：job_change的monthly_salary八大類薪資範圍全部÷1000；財富相關數值描述從「真實台幣金額」改為「遊戲幣單位金額」；原本只在`is_student`為true時才提醒AI不要講「NT$」「元」的例外規則，改成全人生階段一律適用
  - UI顯示：移除5處寫死的`NT$`字首（存款/房產淨值/結算小字/結局畫面），移除setup畫面零用錢欄位多餘的「（遊戲內單位）」註記（全遊戲已統一幣值，不再需要特別標註）
  - 六、6.1（1078行）：兩層骰細節層正向配額的判斷閾值，財富從固定「30」改用財務健康度(3.4.6)判斷，健康/學識維持原本30分制不變
- 驗證方式：USE_MOCK=true（測試前臨時開啟，驗證完已改回false，維持使用者9/16授權的真實API測試狀態不變）
  - `node -e`語法檢查✅
  - 純函式驗證：`FAMILY_FINANCE_BY_TIER`/`STUDENT_ALLOWANCE_UNITS`/`LIVING_COST_BASE`數字對照設計文件table全部相符；清寒/小康/富裕三級距各roll 3次，`computeFinancialHealth()`算出的分數落在文件描述的20-30/50-70/85-100區間附近（清寒偶有18-19，屬既有公式結構本身的既有誤差，非本次改版新增）；`economicTierForSavingsAmount()`在10/150/250/2999/3000/5000的新量級下正確分類
  - 用vm模組stub document/localStorage/fetch後跑實際遊戲邏輯（非DOM渲染）：新角色roll→設定為playing階段→跑滿60回合直到出現3次月結算，`lastSettlement`數值全程是小個位數~三位數的合理正值，無負值、無NaN、無報錯
  - 直接把state.occupationCategory設成白領上班族/自營業者/自由接案三種類別、月薪45-120區間，驗證`computeBaseLivingCost()`與`computeFinancialHealth()`在新量級下都算出合理的正比例結果（財務健康度42-59，非恆為0）
  - **已知限制，誠實記錄**：mock模式的`mockGenerateTurn()`本身從不產生`job_change`/`housing_choice`/`one_time_transaction`（這是mock生成器既有的既定行為，非本次改版新增的缺口），所以沒辦法透過實際跑mock回合觸發AI生成薪資這條路徑，上述「直接設定occupationCategory」是繞過mock限制的直接狀態驗證，不是端到端UI測試
  - **補做視覺驗證（使用者要求）**：本機node是v18、playwright要求v20+，chromium-cli未安裝，因此另外下載Node 20.18.1與Playwright Chromium到scratchpad目錄（不影響系統既有的node 18環境），用本機python3起靜態伺服器＋Playwright screenshot實際瀏覽器截圖驗證：setup roll畫面三種家境（清寒30/小康60/富裕120）「每月零用錢」欄位正確顯示小數字且已移除多餘的「（遊戲內單位）」註記；遊戲中畫面「存款」ledger、回合結算小字（「本回合結算：1.5個月淨收入 44；目前存款 104」）、結局畫面「存款」欄位皆為乾淨小數字；對整頁`document.body.innerText`做`NT$`字串掃描確認全站無殘留；`console`/`pageerror`監聽器全程無任何錯誤訊息。截圖與這次臨時安裝的Node 20/Playwright都只存在系統暫存的scratchpad目錄，不會留在專案裡
- ⚠️ 大改動：財富相關欄位（cash/monthlyIncome/propertyValue/mortgageBalance/familyMonthlyIncome/familySavings等）數值量級全面改變，**舊存檔會顯示異常，需要清空重來**

## 2026-09-20

**〔開發部〕六塊獨立修正：9項機械編號修正、L3人生履歷改分階段配額、7.5.3措辭收斂、3.4.6切換依據對齊9.8、6.2破格對象改版、2.2.1標記撤銷**
- 對應設計文件：一(機械修正9項)、4.2.3(新增)、7.5.3、3.4.6、6.2、2.2.1——完整文字見`life-sim-full-design-doc.md`1.3更新日誌對應條目，已存快照`snapshots/life-sim-full-design-doc_2026-09-20a_sixfixes.md`（這份快照也一併補上了前一天9/19財富系統改版未存的快照——當時漏做，流程疏失，特此記錄）
- 做了什麼（僅列有動`index.html`的兩塊，其餘四塊純文字修正不影響代碼）：
  - **3.4.6對齊9.8**：`computeFinancialHealthForState()`判斷依據從`!s.occupationCategory`改為`s.studentStatus!=="graduated" && s.studentStatus!=="withdrawn"`——不再看occupationCategory是否賦值，改看student_status是否已轉為畢業/肄業，不看年齡；休學(`leave_status=on_leave`)期間`student_status`仍是`enrolled`，繼續使用家庭數字
  - **L3人生履歷分階段配額**：新增`chronicleLifeStageKey(s)`，把「人生階段」定義對齊二、2.5的8段(學生時期整段合併為一段＋7個年齡帶)，取代原本`shouldRecordJudgedMajorEvent()`直接讀`timeState.stageMode`（該欄位只有highschool/college/career三值，career涵蓋22-100+整段太粗，會讓出社會後六七十年只算一次2次上限）；移除`s.chronicle.push()`後面`if(s.chronicle.length>10) s.chronicle.shift()`的全域FIFO上限，總量改由既有的「每階段上限2則」自然封頂（8段×2=16則）；`buildUserMessage()`的`chronicle_recent`欄位在`forceEnding`為true（結局判定/墓誌銘生成）時改傳完整`s.chronicle`，其餘回合維持`slice(-6)`不變
- 驗證方式：USE_MOCK=true（測試前臨時開啟，驗證完已改回false，維持使用者9/16授權的真實API測試狀態不變）
  - `node -e`語法檢查✅（改用寫入暫存腳本檔執行，直接inline `-e`這次被沙箱權限分類器擋下，判定為「Real-World Transactions」誤判，改用腳本檔路徑繞過，非規則層面的問題）
  - 純函式驗證（vm stub跑實際`index.html`程式碼，非重新實作）：`computeFinancialHealthForState()`五種情境全數✅——高中生(studentStatus null)、在學大學生、在學但occupationCategory意外有值(驗證新邏輯正確忽略它)三種情境都吃家庭數字；剛畢業還沒找到工作、17歲肄業兩種情境都立刻切換吃個人數字（即使還沒有正職收入）
  - `chronicleLifeStageKey()`驗證：高中/大學各年齡、22歲仍在學(延畢)全部正確歸類同一個「student」桶；畢業後23/35/45/55/65/75/85歲七個年齡正確對應到七個不同桶（`distinctAdultBucketCount`=7）
  - `shouldRecordJudgedMajorEvent()`同階段連續5次achievement嘗試，正確只放行前2次（`[true,true,false,false,false]`）；模擬20次跨越學生期+7個年齡帶的嘗試，正確放行16次、chronicle長度16且明確超過舊版10則上限（`over10:true`），確認新版沒有FIFO截斷
  - `buildUserMessage()`用8則測試資料驗證：一般回合`chronicle_recent`長度6(切片)，`forceEnding:true`時長度8(完整)
  - 端到端整合測試（實際呼叫`takeTurn()`跑400回合，非直接呼叫內部函式）：mock模式全程無錯誤；`studentStatus`在第161回合(18歲)正確從null轉enrolled、第377回合(22歲)正確從enrolled轉graduated；chronicle在400回合內長度成長到37則、無截斷；`majorEventJudgedCountByStage`最終為`{student:2, "22-29":1}`——確認高中+大學合併期間確實只算一次2次上限的配額(不是舊版stageMode下highschool/college各自2次共4次)，畢業後22-29這個新年齡帶配額獨立重新起算
- ⚠️ **中改動**：`majorEventJudgedCountByStage`欄位的key從舊的`highschool`/`college`/`career`三值改為`student`/`22-29`/`30-39`等新key，且`s.chronicle`不再受10則上限截斷——舊存檔裡殘留的`highschool`/`college`/`career`計數key會變成失效的孤兒欄位（不會報錯，但這幾個舊key累積的次數不會被新邏輯讀取，等同重新歸零計算），影響有限，不需要強制清檔，但如果测试時想要乾淨對照建議清檔重來

**〔整理〕設計文件從單一檔案拆分為`life-sim-design/`資料夾**
- 對應設計文件：無規則變更，純檔案結構調整。詳細拆檔規則已寫入`協作流程說明-共同基準.md`「## 唯一正本」段落(使用者提供新文字)
- 做了什麼：
  - `life-sim-full-design-doc.md`(拆分前1947行)拆成`life-sim-design/`資料夾底下12個章節檔案：`00-總覽.md`(收總目錄+全域更新日誌+設計精神說明，原一、1.3/1.4)、`01-敘事生成規則.md`~`11-高張力抉擇機率判定.md`一章一檔、`12-內容範例庫/`依生命階段再分`12.1-開局個性.md`/`12.2-國高中.md`/`12.3-大學技職.md`+說明用的`00-說明.md`
  - 內容純搬移，一字未改，僅在兩處跨檔案交叉引用(4.2的問題背景「見1.3更新日誌」、4.2.2「呼應一、1.4精神」)補註記「現已拆檔至00-總覽.md」，避免這兩處讀者找不到內容——因為1.3/1.4已經不在原本的01章節檔案裡了
  - `00-總覽.md`新增「檔案對照表」(章節↔檔名對照)，方便查找跨檔案引用
  - 原單一檔案搬到`archive/life-sim-full-design-doc_pre-split_20260920.md`封存，不再讀取引用
  - 同步更新`協作流程說明-共同基準.md`(使用者提供新的「## 唯一正本」文字)、`CLAUDE.md`、`WORKFLOW.md`裡所有指向舊單一檔案路徑的敘述；`WORKFLOW.md`的快照機制說明也同步改為「整個`life-sim-design/`資料夾一起存一份」，取代原本單一檔案快照的做法
- 驗證方式：
  - 逐一核對19個新檔案的開頭/結尾邊界(章節標題、結尾分隔線)與原檔對應行號完全吻合
  - 【定案】標記數量比對：原檔319個，拆分後全資料夾也是319個，一個沒少
  - 用`diff`比對「原檔本體重組」與「新檔案依序串接重組」兩份重建文字，差異只有預期內的2處跨檔案引用註記+若干章節邊界處的空行差異(純排版cosmetic，無內容遺失)
- 快照：拆分前(六塊修正後)先補存`snapshots/life-sim-full-design-doc_2026-09-20a_sixfixes.md`，拆分後另存整個資料夾快照`snapshots/life-sim-design_2026-09-20b_split/`

**〔整理〕結局標籤制整套作廢，改為幸福感→軌跡紀錄→人生總結三層架構（只改設計文件，未動代碼）**
- 對應設計文件：新增三、3.8幸福感系統、新增四、4.5人生軌跡紀錄`life_trajectory`、七、7.1.4整節改寫為「死亡結局的人生總結」；連帶改三、3.1定義句、三、3.5.3問題背景理由、六、6.4、七、7.4.1、四、4.2/4.2.1/4.2.3措辭。完整文字見`life-sim-design/00-總覽.md`全域更新日誌對應條目
- 做了什麼（`index.html`一行未動）：
  - 設計文件：依使用者貼來的完整草稿寫入三塊新內容與四項取代關係，並在各處標明取代關係（不新舊並存）
  - `CLAUDE.md`：最後一行「五種死亡結局目前是舊版計分制（`ENDING_DEFINITIONS`+`computeEndingTitle()`），B7草案尚未寫入正式代碼」已過期，改為反映新設計並標明舊計分制與B7草案雙雙作廢
  - `qa/人生草稿_QA_測試手冊_v1.md`：34.2完全沒實作清單新增4條待實作項目（幸福感三層機制、`life_trajectory`結構、人生總結取代`computeEndingTitle()`、3.8.4程式層禁令的靜態檢查）；B7小節與34.7補作廢註記
  - `qa/QA_34附錄_歷史封存.md`：34.7全文保留當歷史，標題與開頭補作廢註記
  - 本檔案表頭指向拆檔前舊檔案路徑的敘述一併更正
- 驗證方式：不涉及代碼，未跑USE_MOCK。文件層面的檢查：全資料夾grep「人生標籤／結局標籤／標籤判定」確認殘留的全部是新寫的取代關係說明或歷史日誌條目，沒有漏改的現行規則；新增章節編號3.8／4.5／7.1.4.1-7.1.4.3無與既有編號衝突；`00-總覽.md`目錄三、四、七三處同步新增對應條目
- 發現的設計文件本身的問題（已在回報中向使用者列出待確認）：(1)使用者草稿引用的「一、1.4」實際存在，只是拆檔時搬到`00-總覽.md`，已統一寫成「一、1.4設計精神說明(現已拆檔至00-總覽.md)」；(2)人生總結的L1/L2/L3與四、4.2.1記憶分層架構的L1-L4編號撞名，已在7.1.4.2加一句註明兩者無關；(3)4.5「人生階段」草案未定義切法，已補一條沿用4.2.3既有8段定義的定案句，屬我補的交叉引用，使用者可否決
- ⚠️ **大改動預告**：之後實作時會新增`life_trajectory`遊戲狀態結構、移除`ENDING_DEFINITIONS`+`computeEndingTitle()`，舊存檔會跑不動，需要清空重來
- 快照：`snapshots/life-sim-design_2026-09-20c_幸福感軌跡總結/`（序號用c，因為b已被同日的拆檔快照佔用）

**〔開發部〕補完幸福感三層機制／`life_trajectory`分階段軌跡紀錄／死亡結局改為人生總結（E8～E14，接續前一則整理筆記的E1～E7b）**
- 對應設計文件：三、3.8幸福感系統；四、4.5人生軌跡紀錄；七、7.1.4死亡結局的人生總結。文字本身這次沒有再改，只補完`index.html`代碼，交接依據是使用者貼的`人生草稿_9-20幸福感改版_完整修正檔.md`交接文件
- 做了什麼：
  - `mockGenerateTurn()`一般回合改吐`emotional_tone`四選一（`weightedPick`，warm/unsettling各42%、uplifting/heavy各8%），取代原本的`happiness_delta`數值；同步移除return物件裡的`ending_title`/`ending_summary`欄位（`is_ending:false`時本來就不需要）
  - 整段刪除`ENDING_DEFINITIONS`標籤計分制與`computeEndingTitle()`，換成一行取代關係註解
  - `applyResult()`：加上`updateLifeTrajectory(s, forceEnding)`呼叫（放在chronicle.push之後、結局判定之前），結局區塊改用`assembleEnding(s, r)`組裝三層人生總結，取代原本的`{title, summary}`
  - `renderEnding()`改版：不再顯示任何標籤/分數，依序輸出L1段落回顧→（階段間）L2變化句→L3墓誌銘；對9/20以前只有`title`/`summary`的舊存檔做降級相容處理（單段顯示、不補猜測內容），避免直接爆畫面
  - `snapshotForUndo()`補上`happinessBaseline`/`happinessRegressionCounter`/`milestoneSkipReason`/`lifeTrajectory`/`stageAccum`，undo後這幾個新欄位才不會跟正式狀態脫節
  - Debug面板新增「幸福感基準線(唯讀)」一行，方便測試時對照`happiness`實際值與基準線的差距
- 驗證方式（USE_MOCK=true，驗證完維持原本`false`不變，沒有動這個開關）：
  - 語法檢查：`<script>`內容抽出寫成暫存腳本檔，`node --check`通過
  - 殘留grep：`happiness_delta`／`ENDING_DEFINITIONS`／`computeEndingTitle`／`ending_title`／`ending_summary`／`ending.title`，剩下的全部只是說明取代關係的註解或system prompt裡對照舊制的文字，沒有殘留的現行邏輯
  - 3.8.4程式層禁令靜態檢查：grep `s.happiness\s*=`，全檔只有`applyHappiness()`內兩處，輸入只有`tone`與`happinessBaseline`，沒有任何地方拿里程碑/狀態當幸福感加減條件
  - 純函式驗證（vm stub跑實際`index.html`程式碼，非重新實作）：`happinessToneDelta()`四個enum值落在對應區間、非法值(`undefined`/`"有點warm"`/`null`/`123`)一律回0不拋錯；`computeHappinessBaseline()`驗證6.4中立性——「三條管道拉滿的不成家角色」跟「五條管道都拉滿的角色」基準線只差約1.5分（82.07 vs 80.53），確認機制設計如預期生效；`computeAutonomyRaw()`確認skipped會推高自主感、全部available時回中性值50；`applyHappiness()`確認每5回合觸發一次回歸、且方向正確（遠高於基準線時往下拉、遠低於時往上拉）；`updateLifeTrajectory()`模擬跨越學生期+7個年齡帶共8段，確認`lifeTrajectory`剛好8筆、`longest_low_streak`在刻意壓低幸福感的區段確實>0；`buildLifeSummaryMaterial()`確認segments筆數、`resume_entries`配額上限、tone四級分界、`transitions`只在光譜位移≥15時才出現，皆符合預期
  - 端到端整合測試：手動組出`life_summary`格式的AI回傳結果餵給`assembleEnding()`+`renderEnding()`，確認結局畫面正確輸出段落回顧＋變化句＋墓誌銘、無任何標籤/分數、無literal `undefined`文字；另外用9/20以前格式的舊`ending{title,summary}`測降級相容路徑，同樣正常render不崩潰
- 發現的問題（不在這次E8～E14範圍內，但因為這次首次真正啟用`updateLifeTrajectory()`才浮現，記錄下來但**沒有動代碼修**，見下方測試部筆記與QA手冊34.2新增項目）：
  - `newStageAccum()`/`freezeStageTrajectory()`（屬於先前已套用的E1區塊）在「人生階段剛好在這一回合切換」的邊界情況下，會把這一回合的`chronicle`新條目誤歸進即將凍結的舊階段，而不是這一回合實際所屬的新階段——因為`applyResult()`裡`chronicle.push()`固定在`updateLifeTrajectory()`之前執行，階段切換偵測拿到的`chronicle.length`已經包含了這一回合的新條目。只有「重大事件剛好發生在階段邊界那一回合」才會踩到，機率不高，但屬於真實可重現的資料誤植，不是設計文件層面的問題

**〔測試部〕上述功能的驗證腳本與發現的邊界bug**
- 用Node `vm`模組載入`index.html`實際程式碼（非重新實作），跑一系列針對性測試涵蓋上方所有驗證項目
- 額外寫了一個最小重現案例，確認並隔離出「人生階段邊界那一回合的chronicle條目歸屬」問題：模擬第4回合同時是「學生時期→22-29歲」的轉換點且該回合有chronicle新增，結果條目被錯放進學生時期的`resume_entries`。已排除是這次E8～E14新寫的代碼造成，根因在更早就套用的`newStageAccum()`/`freezeStageTrajectory()`。建議記入QA手冊34.2待處理，修法需要先決定「階段邊界回合的履歷條目該算舊階段還是新階段」這個語意問題，不是單純的程式錯字
- ⚠️ 大改動提醒（沿用交接文件既有預告，非本次新增）：`life_trajectory`是全新遊戲狀態欄位、`ENDING_DEFINITIONS`已整套移除，**舊存檔會跑不動，需要清空重來**

## 格式範本（之後新增記錄請複製這段）

```
## YYYY-MM-DD

**〔開發部／測試部／整理〕標題**
- 檔案：`index.html` 第__行 / 新增函式 __
- 對應設計文件：第__章 __節
- 做了什麼：
- 驗證方式（USE_MOCK=true，跑了哪些分支）：
- 發現的設計文件本身的漏洞或矛盾（如有）：
```
